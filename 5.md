LabReportParser V3.0 软件详细设计说明书
文档信息
表格
项目	内容
项目名称	LabReportParser V3.0
中文名称	医学检验报告智能解析与管理平台
英文名称	Laboratory Report Intelligent Parsing and Management Platform
文档类型	Software Design Specification（SDS）
文档版本	V3.0
Volume	Volume 1
编写语言	Markdown
数据库	MySQL 8.0
后端	Python 3.12 + FastAPI
前端	Vue3 + TypeScript
OCR	PaddleOCR
PDF 解析	PyMuPDF + pdfplumber
图片处理	OpenCV
作者	ChatGPT
创建日期	2026-07-14
修订记录
表格
日期	版本	修改内容
2026-07-14	V3.0	初始版本
目录
第一章 项目概述
第二章 需求分析
第三章 系统总体架构
第四章 技术选型
第五章 系统模块设计
第六章 OCR 引擎详细设计
第七章 PDF 解析详细设计
第八章 医院识别与 Parser 解析引擎设计
第九章 数据标准化引擎设计
第十章 MySQL 数据库详细设计
第十一章 REST API 设计
第十二章 前端系统设计
第十三章 Parser 解析引擎详细设计
第十四章 文件存储与缓存设计
第十五章 人工审核（Review）系统设计
第十六章 系统安全设计
第十七章 性能优化设计
第十八章 部署架构设计
第十九章 AI 扩展设计（V4.0 规划）
第二十章 系统测试设计
第二十一章 运维与监控设计
第二十二章 项目实施与版本规划
附录
第一章 项目概述
1.1 项目背景
随着我国医疗信息化建设不断推进，各级医院均已实现检验报告电子化。目前主流检验报告以 PDF、JPG、PNG 等格式分发，但各医院 LIS 厂商不统一，带来如下痛点：
页面布局差异巨大
检验项目命名不统一（缩写 / 中文 / 英文混用）
单位、参考范围、异常标记格式不统一
PDF 分为可复制文字版与纯扫描图片版
手机拍照图片画质参差不齐
示例：同一指标多医院命名差异
表格
医院	项目名称
医院 A	ALT
医院 B	GPT
医院 C	谷丙转氨酶
医院 D	Alanine Aminotransferase
多源异构报告无法直接统计分析，因此需要一套标准化智能解析平台统一处理各类化验单。
1.2 项目目标
1.2.1 自动多格式解析
支持文件：PDF（文字 / 扫描）、JPG、JPEG、PNG、BMP、TIFF、WEBP；上传后自动识别文件类型，无需人工指定解析模式。
1.2.2 全自动 OCR 处理
扫描件、图片自动调用 PaddleOCR，输出完整结构化 OCR 数据：原始文本、坐标 Bounding Box、识别置信度、页码，全程可追溯。
1.2.3 自动医院识别
依据医院 Logo、名称、页眉、地址、电话、版式模板综合匹配；识别成功自动加载对应专用 Parser；识别失败使用通用解析器 CommonParser。
1.2.4 检验指标自动提取
统一提取字段：项目名、缩写、结果、单位、参考区间、异常标记、采样 / 报告日期、医院、来源页码、图像坐标。
1.2.5 数据自动标准化
内置医学指标字典，统一别名、单位、异常标识。示例映射关系：
表格
原始名称	标准名称	缩写
GPT	谷丙转氨酶	ALT
ALT	谷丙转氨酶	ALT
Alanine Aminotransferase	谷丙转氨酶	ALT
1.2.6 数据自动入库
原始文件、OCR 文本、页面截图、缩略图、标准化结果、原始解析结果全部存入 MySQL，完整数据链路可追溯。
1.2.7 在线双向预览
支持 PDF / 图片在线浏览，指标与原图双向定位，点击表格指标自动高亮原图对应区域；支持缩放、翻页、缩略图导航。
1.2.8 历史趋势分析
对血常规、肝肾功能、血糖、肿瘤标志物等指标生成 ECharts 趋势折线图，长期跟踪指标变化。
1.3 建设原则
模块化：上传、OCR、Parser、标准化、数据库、前端解耦，独立接口通信
插件化：每家医院独立 Parser 插件，新增医院无需修改核心代码，运行时动态加载
配置化：医院模板、指标别名、单位映射全部使用 YAML 配置，支持热更新
可扩展：预留 LLM、MinIO、PostgreSQL、多租户扩展接口
可维护：统一日志、数据模型、错误码、配置规范
高性能：多线程 OCR、Celery 异步任务、Redis 缓存，支持千份报告批量解析
数据可追溯：每条指标绑定原始文件、页码、OCR 坐标，所有修改留版本记录
1.4 系统功能范围
表格
模块	功能
上传中心	文件 / 目录批量上传、拖拽上传、格式过滤
OCR 识别	图片预处理、文字识别、缓存、坐标输出
PDF 解析	文字 PDF 提取、扫描 PDF 转图、混合页兼容
医院识别	多特征匹配、自动匹配解析插件
标准化	指标 / 单位 / 异常标记统一映射
数据管理	MySQL 增删改查、逻辑删除
在线预览	PDF.js 渲染、指标定位高亮
趋势分析	ECharts 多指标时序图表
导出中心	Excel/CSV 批量导出
用户管理	RBAC 权限
配置中心	医院模板、标准指标维护
审计日志	全操作变更记录
1.5 系统边界
1.5.1 系统负责业务
文件管理
单文件 / 多文件 / 整个目录上传、递归扫描子目录、自动过滤非法后缀、ZIP / 共享目录预留扩展。
文件格式处理
支持 PDF、JPG、PNG、BMP、TIFF、WEBP；预留 HEIF、DICOM 截图兼容。
OCR 服务
图片文字识别、OCR 缓存、坐标存储；不自研 OCR 算法，基于 PaddleOCR 封装。
文档解析
提取医院、采样时间、检验指标全套结构化数据。
数据标准化
统一项目名称、单位、参考区间、异常标识。
数据存储与预览
MySQL 持久化、原图 / 缩略图存储、在线双向预览。
数据分析与人工校对
指标趋势展示、OCR / 解析结果人工修改留痕。
数据导出
Excel、CSV 为主，预留 JSON、XML、FHIR 导出。
1.5.2 系统不负责业务
不对接 HIS/LIS/PACS/EMR，不处理挂号、收费、医保、医嘱、检验仪器控制，不提供医疗诊断，仅做报告数据结构化管理。
1.6 使用对象
普通用户：个人化验单上传、查询、导出、健康趋势查看
医生：患者多份报告指标对比、长期变化分析
科研人员：批量导出检验数据用于统计研究
数据管理员：维护标准指标库、医院解析模板
系统管理员：服务器、数据库、缓存、权限运维
1.7 系统业务流程
总体流程图
PDF

图片

纯文字

扫描/混合

上传目录/文件

扫描全部文件

判断文件类型

PDF解析模块

OCR识别

文字PDF判定

直接提取文本

通用/医院Parser解析

自动识别医院

数据标准化

数据质量校验

写入MySQL

生成页面缩略图

在线预览资源生成

指标趋势查询

Excel/CSV导出





















上传时序图
Parser
API
MySQL
Parser任务服务
FastAPI网关
Frontend
User
Parser
API
MySQL
Parser任务服务
FastAPI网关
Frontend
User
拖拽/选择目录上传
分片提交文件
创建异步解析任务
执行OCR/文本提取/标准化
保存报告、指标、OCR数据
推送任务完成状态
展示解析结果列表
预览定位时序图
MySQL
API
Frontend
User
MySQL
API
Frontend
User
点击某条检验指标
请求该指标BoundingBox坐标
查询OCR坐标数据
返回页码、x/y/宽高
坐标信息
PDF页面自动滚动并高亮指标区域
1.8 系统核心特点
多医院自动适配，插件化模板无需改代码
图文 PDF、手机照片统一一套处理流程
OCR 缓存机制，重复文件不重复识别
全链路数据溯源，每条指标绑定原图坐标
指标标准化统一，跨医院数据可对比统计
指标 - 原图双向定位，校对高效
支持上千份文件批量自动处理
预留 LLM 视觉模型扩展接口
1.9 技术量化目标
表格
指标	目标值
清晰扫描件 OCR 识别率	≥98%
医院自动识别准确率	≥99%
检验指标识别完整率	≥99%
标准化匹配成功率	≥98%
普通 PDF 单份解析耗时	≤5 秒
批量处理上限	单次 1000 份报告
单份报告查询响应	≤300ms
10000 条数据 Excel 导出	≤10 秒
1.10 环境兼容性
操作系统
服务端：Ubuntu22+、Debian、Rocky Linux、Windows Server2022
客户端：Windows10/11
浏览器
Chrome、Edge、Firefox，推荐 Chrome 最新版
运行依赖版本
Python：3.11 最低，推荐 3.12
MySQL：8.0，兼容 MariaDB
部署：本地运行、Docker Compose，V4.0 规划 K8s
1.11 本章总结
本章明确项目背景、建设目标、边界、用户、整体业务流程与量化指标，为后续需求、架构设计提供基础依据。
第二章 需求分析
2.1 概述
本章从业务、功能、非功能、性能、安全、数据维度定义系统全部需求，作为开发、测试验收基准。
2.2 业务需求分析
2.2.1 建设目标
自动解析各类医院纸质扫描、电子化验单
区分文字 / 扫描 PDF，自动 OCR 处理图片类报告
统一异构检验指标，构建标准化数据库
提供统一查询、预览、趋势分析平台
预留大模型 AI 扩展能力
2.2.2 现存业务痛点
版式不统一：各医院表格、字段顺序、命名差异极大，无法批量统计
人工录入低效：手动复制 Excel 耗费大量人力，易出错
历史数据割裂：多份报告分散，无法自动生成指标时序趋势
扫描照片无法数字化，只能人工抄写
指标别名混乱，同一种项目无法合并统计
数据无溯源，Excel 数值无法对应原图化验单
OCR 识别错误无校对、无修改记录
2.2.3 标准化业务流程
上传文件 → 自动解析 (OCR / 文本提取) → 标准化映射 → 入库 → 在线查询 / 趋势 / 导出
2.3 功能需求总表
表格
编号	模块	必选
FR-001	上传中心	√
FR-002	OCR 识别	√
FR-003	PDF 解析	√
FR-004	医院模板识别	√
FR-005	数据标准化	√
FR-006	数据管理	√
FR-007	在线预览	√
FR-008	趋势分析	√
FR-009	数据导出	√
FR-010	系统用户管理	√
2.4 FR-001 上传中心需求
支持单文件、多文件、本地整个目录拖拽上传，自动递归子目录
文件白名单过滤，自动过滤 exe/txt/doc 等不支持格式
上传参数可配置：单文件最大 100MB、单次最多 5000 个文件
上传后计算 MD5/SHA256，重复文件跳过解析，实现秒传
2.5 FR-002 OCR 识别需求
自动区分 PDF 类型，文字 PDF 跳过 OCR，图片 / 扫描 PDF 强制 OCR
支持中英文、数字、↑↓H/L 等异常符号识别
输出文本、置信度、页面坐标、页码，持久化存储
内置图片预处理：灰度、去噪、旋转、倾斜校正、自动裁边
OCR 结果本地缓存，相同图片不再重复识别
2.6 FR-003 PDF 解析需求
双引擎协同：PyMuPDF 判断页面类型，pdfplumber 提取表格文字
逐页判定文字 / 扫描 / 混合页面，每页独立处理
多页长 PDF 完整解析，渲染高清预览图、缩略图
2.7 FR-008 检验指标提取需求
强制提取字段：项目原始名、标准缩写、结果、单位、参考上下限、异常标记、采样日期、医院、来源文件、页码、OCR 坐标；同时支持阴性 / 阳性定性结果存储。
2.8 FR-005 标准化需求
项目别名统一映射（GPT=ALT = 谷丙转氨酶）
单位统一：IU/L→U/L，兼容换算公式
异常标识统一：↑/High/H 统一为 H，↓/Low/L 统一为 L，正常 = N
参考区间拆分为数字下限、上限，便于数值统计
2.9 非功能需求 NFR
可用性 NFR-001：全年可用率≥99.9%，支持 7×24 小时批量任务运行
易用性 NFR-002：用户无需了解 OCR、PDF 底层逻辑，上传即出结果
可维护 NFR-003：全系统模块化低耦合，独立单元可单独迭代
可扩展 NFR-004：OCR、数据库、医院模板均可插拔替换
可配置 NFR-005 全部参数放入 yaml / 数据库，禁止硬编码
可测试 NFR-006 核心模块单元测试、接口自动化全覆盖
可部署 NFR-008 Windows/Linux、Docker 一键部署
2.10 性能需求
OCR 单页≤0.8s (GPU)、多线程并行
报告列表分页查询≤300ms，趋势查询≤500ms
十万条数据 CSV 导出≤5s，万条 Excel≤10s
百万级指标数据表仍保持亚秒级查询（合理索引）
2.11 安全需求
鉴权：JWT 登录，Access+Refresh 双令牌
权限：RBAC 角色隔离（管理员 / 医生 / 普通 / 只读）
文件上传黑名单拦截可执行程序
ORM 参数化查询防 SQL 注入，前端输入 HTML 转义防 XSS
所有修改操作写入审计日志，记录前后变更、操作人 IP
2.12 核心数据实体
upload_task：上传任务总表，记录批量文件进度、状态
report：单份报告主信息（医院、采样时间、页数）
report_page：每页图像、预览图、OCR 关联
ocr_block：单块文字坐标、置信度
lab_item_dictionary：标准检验项目别名库
lab_result：核心指标数据表（原始 + 标准化双存）
hospital/hospital_parser：医院与对应解析模板
audit_log：全变更审计记录
2.13 用户用例 UC
UC-001 用户上传本地文件夹，系统自动批量解析入库
UC-002 上传图片化验单，OCR 结构化提取指标
UC-003 查看报告，点击指标自动定位原图对应位置
UC-004 人工修正识别错误，系统留存修改日志
UC-005 选择单一指标生成多年趋势图表并导出
2.14 业务约束规则 BR
BR-001 一份报告对应一条 report 记录，一页对应一条 report_page
BR-002 原始解析数据与标准化数据分开存储，不可覆盖原始值
BR-003 人工修改不删除原始 OCR 文本，全变更留痕
BR-004 文件 MD5 重复直接跳过解析，避免重复计算
BR-005 识别异常数据自动进入人工审核队列，不丢弃
2.15 本章总结
本章完整覆盖业务痛点、功能、性能、安全、数据约束，作为项目开发与验收标准。
第三章 系统总体架构
3.1 概述
系统采用四层前后分离架构，分层解耦、低耦合、单一职责，模块化 + 插件化设计。
3.2 四层总体架构图
API网关层

前端表现层

HTTPS REST API

Vue3 + TS 浏览器端

鉴权/参数校验/路由分发

上传服务

解析核心服务

查询统计服务

文件管理

PDF引擎

OCR引擎

医院解析器

标准化引擎

MySQL数据库

本地文件存储
















分层职责：
表现层：Vue3+TS 前端页面交互
API 网关层：统一接口、JWT 鉴权、参数校验
业务服务层：上传、OCR、PDF、Parser、标准化、趋势计算
持久层：MySQL 数据库 + 本地文件存储
3.3 架构设计原则
单一职责 SRP：每个模块只做一件事，上传不参与解析
开闭 OCP：新增医院 Parser 仅新增插件，不修改旧代码
依赖倒置 DIP：业务依赖抽象基类，不绑定具体实现
插件化：医院解析器独立插件，启动自动加载注册
配置驱动：业务规则全部 YAML 配置，无硬编码
3.4 一级系统模块划分
plaintext
LabReportParser
├── Upload 上传模块
├── Task 异步任务管理
├── File 文件统一管理
├── OCR 文字识别引擎
├── PDF PDF解析引擎
├── Parser 化验单解析核心
├── Hospital 医院识别
├── Normalize 标准化引擎
├── Preview 在线预览
├── Export 导出服务
├── Trend 趋势分析
├── User 用户权限
├── API 接口层
├── System 系统配置
├── Audit 审计日志
└── Monitor 系统监控
3.5 模块依赖流向
Upload

Task

File

PDF

OCR

Parser

Hospital

Normalize

数据校验

MySQL

Preview

Export

Trend

Audit















3.6 统一解析流水线
输入文件 → 文件加载 → PDF / 图片分类 → OCR / 文本提取 → 医院识别 → Parser 字段提取 → 标准化 → 数据校验 → 入库 → 生成预览资源
3.7 后端目录结构
text
LabReportParser/
├── backend/
│   ├── app/
│   │   ├── api/          # 所有路由接口
│   │   ├── core/         # 全局工具、鉴权、配置
│   │   ├── models/       # SQLAlchemy数据库模型
│   │   ├── schemas/      # Pydantic请求响应模型
│   │   ├── services/     # 业务服务实现
│   │   ├── parsers/      # 医院Parser插件
│   │   ├── ocr/          # OCR封装层
│   │   ├── normalize/   # 标准化逻辑
│   │   ├── export/       # Excel/CSV导出
│   │   ├── preview/      # 预览生成
│   │   ├── hospital/     # 医院识别
│   │   ├── task/         # Celery异步任务
│   │   └── utils/        # 通用工具
│   ├── config/           # yaml配置文件
│   ├── scripts/          # 初始化脚本
│   ├── tests/            # 单元测试
│   ├── storage/          # 文件存储根目录
│   └── main.py           # FastAPI入口
├── frontend/             # Vue前端工程
├── docker/               # Docker部署脚本
├── docs/                 # 项目文档
├── sql/                  # 建表SQL脚本
└── README.md
3.8 本章小结
系统分层模块化、插件化，低耦合易扩展，适配大批量化验单解析、多医院兼容、后期 LLM 模型接入需求。
第四章 技术选型
4.1 选型原则
开源成熟、社区活跃、跨平台、轻量化、易容器部署。
4.2 全栈技术栈总表
表格
层级	技术
前端框架	Vue3 + TypeScript + Vite
UI 组件	Element Plus
图表	ECharts
PDF 预览	PDF.js
后端	FastAPI（ASGI 异步）
ORM	SQLAlchemy 2.x
数据校验	Pydantic v2
数据库	MySQL 8.0
OCR	PaddleOCR
PDF 处理	PyMuPDF + pdfplumber
图像处理	OpenCV
异步任务	Celery
缓存 / 消息队列	Redis
日志	Loguru
容器	Docker Compose
API 文档	Swagger 自动生成
4.3 后端选型说明
Python 3.12：asyncio 完善，FastAPI 官方推荐，完美兼容 PaddleOCR
FastAPI：自动接口文档、强类型校验、高并发异步，优于 Flask/Django
MySQL8.0：支持 JSON、事务、utf8mb4，支撑百万级检验数据
PaddleOCR：中文化验单识别强，输出坐标文本一体化
PyMuPDF+pdfplumber：MuPDF 快速渲染，pdfplumber 精准提取表格，互补
Redis+Celery：耗时解析任务异步队列，缓存字典提升速度
4.4 前端选型说明
Vue3+TS 保证类型安全，PDF.js 浏览器直接预览化验单，ECharts 绘制指标趋势。
4.5 文件存储设计
默认本地分级目录存储文件，二进制不入库；预留 MinIO / 阿里云 OSS 适配器，无需修改业务代码。
4.6 本章小结
整套技术轻量化、易部署，兼顾批量解析性能，预留 AI、对象存储扩展接口。
第五章 系统模块设计
5.1 概述
分模块定义职责、输入输出、内部逻辑、对外接口。
5.2 Upload 上传模块
职责：文件校验、MD5/SHA256 去重、原始文件保存、创建异步解析任务。
流程：前端提交文件 → 校验后缀大小 → 哈希查重 → 写入 storage → 创建 Task 任务入库。
5.3 Task 任务模块
任务生命周期：Created → Waiting → Running → Success/Failed；支持暂停、重试、取消；记录文件总数、成功失败数量、进度百分比。
5.4 File 文件管理模块
统一封装文件读写、缩略图生成、日期分目录存储；业务禁止直接操作磁盘路径，统一调用 StorageManager。
5.5 OCR 模块
见第六章完整设计。
5.6 PDF 模块
见第七章完整设计。
5.7 Parser 解析模块
见第十三章完整设计。
5.8 Hospital 医院识别模块
见第八章完整设计。
5.9 Normalize 标准化模块
见第九章完整设计。
5.10 Preview 预览模块
生成每页缩略图、高清预览图；提供坐标查询接口，实现指标原图双向定位。
5.11 Export 导出模块
支持 Excel/CSV 多 Sheet 报表；大文件异步生成，防止接口超时。
5.12 Trend 趋势模块
按指标、时间聚合历史数值，输出 ECharts 标准时序 JSON。
5.13 User 用户模块
JWT 双令牌登录，BCrypt 加密密码，RBAC 角色权限控制。
5.14 System 系统配置模块
OCR、上传、模板等参数全部配置化，支持后台热更新。
5.15 Audit 审计模块
记录所有新增、修改、删除操作，存储变更前后 JSON、操作人、IP、时间。
5.16 Monitor 监控模块
采集 CPU、内存、OCR/Parser 成功率、队列长度等指标可视化展示。
5.17 模块统一约束
所有业务数据最终存入 MySQL，数据库为唯一可信数据源，查询不重复解析原始文件。
第六章 OCR 引擎详细设计
6.1 概述
OCR 处理扫描 PDF、手机照片，输出带坐标、置信度结构化文本，是扫描报告核心数据源。
6.2 OCR 完整流水线
图片加载 → OpenCV 预处理 → 方向 / 倾斜校正 → 文字检测识别 → 文本清洗 → 缓存写入 → 输出 OCRBlock 数组
6.3 OCR 内部组件
plaintext
OCRService
├── ImageLoader 图片读取
├── ImagePreprocessor 图像增强
├── OrientationDetector 旋转校正
├── OCRAdapter PaddleOCR适配器
├── OCRPostProcessor 文本清洗
├── OCRCache 磁盘缓存
└── OCRValidator 数据校验
6.4 图片预处理步骤
自动读取图像
自动旋转至正向
灰度化
高斯去噪
自适应二值化
倾斜矫正
裁去四周空白白边
6.5 OCRBlock 标准数据结构
json
{
  "page": 1,
  "block_index": 15,
  "text": "谷丙转氨酶(ALT)",
  "confidence": 0.998,
  "bbox": {"x": 128, "y": 342, "width": 62, "height": 21},
  "rotation": 0,
  "language": "zh"
}
bbox 坐标是指标双向定位核心，永久入库保存。
6.6 OCR 缓存机制
缓存键 = 文件 SHA256 + 页码，缓存 JSON 持久化磁盘；相同文件不再重复识别，大幅提升批量速度。
6.7 多线程并行
线程数可配置，推荐 CPU 核心 * 2；支持 GPU CUDA 加速。
6.8 OCR 后处理规则
配置 ocr_replace.yaml 修正 OCR 常见混淆字符：O→0、I→1、l→1、S→5；清除多余空格、换行。
6.9 异常处理
图片损坏、超时、内存溢出自动捕获；单页失败不中断整份任务，标记待人工复核。
6.10 OCR 配置示例 yaml
yaml
ocr:
  engine: paddle
  language: chinese
  use_gpu: false
  thread_num: 8
  enable_cache: true
  confidence_threshold: 0.85
  auto_rotate: true
  auto_deskew: true
6.11 性能优化手段
缓存复用识别结果
超大图片自动缩放降分辨率
文字 PDF 直接跳过 OCR 流程
批量任务放入 Celery 异步队列
第七章 PDF 解析详细设计
7.1 概述
自动区分纯文字 PDF、扫描 PDF、混合 PDF，分路径处理，最大化解析速度。
7.2 PDF 分层组件
plaintext
PDFService
├── PDFLoader 文件加载
├── PDFAnalyzer 页面统计
├── PageClassifier 页面分类判定
├── TextExtractor 文字/表格提取
├── PDFRenderer PDF转高清图片
├── PDFCache 渲染缓存
└── PDFExporter 统一Page对象输出
7.3 三种页面类型定义
TEXT 文字页：文本数量 > 100，可直接复制，无需 OCR
IMAGE 扫描页：文字极少，整页图片，必须 OCR
MIXED 混合页：同时存在文字 + 图片，双数据源合并解析
7.4 页面判定算法
综合页面文字总长度、图片总面积、字体数量打分，阈值可配置。
7.5 PDF 渲染规则
扫描页默认 300DPI 生成 PNG 预览图，存入缓存目录，重复读取不重复渲染。
7.6 文本提取能力
pdfplumber 提取带坐标表格文本，输出结构与 OCRBlock 统一，Parser 可无差别消费文字 PDF/OCR 数据。
7.7 异常兼容
加密 PDF 给出提示；损坏、超大分页 PDF 捕获异常标记人工处理。
7.8 PDF 统一输出 Page 模型
包含页码、宽高、页面类型、预览路径、文本总量、OCR 执行状态，供 Parser 统一读取。
第八章 医院识别与 Parser 解析引擎设计
8.1 整体流程
OCR / 文本数据 → HospitalDetector 识别医院 → Parser 工厂匹配对应插件 → 执行字段提取
8.2 医院多特征分级匹配（权重从高到低）
一级：完整医院名称文本匹配
二级：医院 Logo 图片哈希匹配
三级：地址、固定联系电话匹配
四级：页眉、表格版式特征匹配
综合得分最高为匹配医院；无匹配自动使用通用 CommonParser。
8.3 插件目录规范
text
plugins/hospital/
    peking_union/
        parser.py    # 专属解析类
        config.yaml  # 表格/关键词配置
    ruijin/
    west_china/
    common/
程序启动自动扫描全部插件并注册。
8.4 BaseParser 统一基类规范
所有医院 Parser 必须实现统一接口：
parse_header ()：解析医院、采样时间等表头信息
parse_table ()：循环提取检验表格行数据
parse_footer ()：过滤页脚无关文字
统一输出标准化 LabResult 对象，上层业务无感知切换模板。
8.5 Common 通用解析器
无匹配医院兜底方案，依靠关键词、坐标位置自动识别表格列，适配无模板小众医院。
8.6 Parser 生命周期
识别医院→工厂实例化 Parser→执行解析→释放实例，不常驻内存。
8.7 模板版本管理
每个医院支持多版本模板，数据库记录版本号，支持回滚旧规则。
8.8 扩展性
新增医院仅新增插件文件夹，无需修改核心业务代码，支持配置热加载。
第九章 数据标准化引擎设计
9.1 作用
消除各医院命名、单位、格式差异，原始数据完整保留不覆盖。
9.2 标准化流水线
原始解析结果 → 项目别名映射 → 单位统一 / 换算 → 参考区间拆分 → 异常标记统一 → 数据合法性校验
9.3 项目名称标准化
数据库 lab_item_dictionary 存储标准名称、缩写、中英文别名数组，支持后台 Excel 批量导入。
示例映射：GPT / ALT / 谷丙 → 标准名称：谷丙转氨酶，缩写 ALT。
9.4 单位标准化
unit_dictionary 存储映射与换算系数，例如 IU/L 统一为 U/L，mg/dL 可换算 mmol/L；同时保存 raw_unit、standard_unit 两套值。
9.5 参考区间标准化
自动解析9~50、<40、阳性等格式，拆分为 reference_low/reference_high 数字字段，方便数值统计分析。
9.6 异常标记统一
↑/High/H → H；↓/Low/L → L；正常 = N；无法区分高低标记 A。
9.7 Redis 缓存优化
ALT/AST/GLU 等热门指标映射规则存入 Redis，减少数据库频繁查询。
9.8 数据质量评分
每条指标综合 OCR 置信度、匹配准确度计算质量分，低分自动进入人工审核队列。
9.9 V4.0 自动学习规划
管理员审核新增别名可一键写入标准字典，持续迭代完善词库。
第十章 MySQL 数据库详细设计
10.1 基础配置
数据库名：lab_report
字符集：utf8mb4
排序规则：utf8mb4_general_ci
时区：Asia/Shanghai
不强制数据库外键，使用逻辑关联提升写入性能。
10.2 E-R 简图
创建

包含报告

每页

OCR文本块

检验指标

归属医院

标准化映射

操作记录

人工审核

USER

UPLOAD_TASK

REPORT

REPORT_PAGE

OCR_BLOCK

LAB_RESULT

HOSPITAL

LAB_ITEM_DICTIONARY

AUDIT_LOG

REVIEW_TASK




















10.3 核心数据表
1. sys_user 用户表
id、username、bcrypt 密码、nickname、role_id、登录时间、状态
2. upload_task 上传任务
task_uuid、总文件、成功失败数量、进度、起止时间、操作用户
3. report 报告主表
report_uuid、医院 id、采样时间、报告时间、页数、parser 名称、质量得分、逻辑删除标记
4. report_file 原始文件
原文件名、UUID 存储名、md5/sha256、文件大小、存储路径，用于文件去重
5. report_page 页面表
页码、宽高、预览图路径、OCR 状态，关联 report_id
6. ocr_block OCR 文本块
page_id、文本、置信度、x/y 宽高坐标、旋转角度
7. hospital 医院表
医院名称、简称、地区、对应 parser、启用状态
8. hospital_parser 模板版本
医院 id、模板版本、配置文件路径、发布时间
9. lab_item_dictionary 标准指标库
标准名称、缩写、英文、分类、别名 JSON 数组
10. unit_dictionary 单位映射
原始单位、标准单位、换算系数、偏移量
11. lab_result（核心指标表）
每条检验结果一条记录：report_id、page_id、ocr_block_id、原始名称、标准名称、原始结果、标准化数值、原始单位、标准单位、参考上下限、异常标记、坐标、审核状态、质量分。原始数据与标准化数据双存储，全程可追溯。
12. review_task 人工审核任务
指标 id、优先级、审核人、审核结果、处理时间
13. audit_log 审计日志
模块、操作类型、业务 ID、操作人、IP、修改前后 JSON、操作时间
14. system_config 系统配置
全局 OCR、上传、导出等参数，后台可动态修改
10.4 索引设计
lab_result：idx_abbreviation、idx_sample_time、idx_hospital、idx_abnormal
report：idx_hospital、idx_sample_time
ocr_block：idx_page
upload_task：idx_status
10.5 数据分区规划
百万级数据按年份分区，5 年以上数据归档归档库，提升查询速度。
10.6 备份策略
每日全量备份，实时 binlog，支持任意时间点数据恢复。
第十一章 REST API 设计
11.1 通用规范
统一接口前缀 /api/v1/
全部返回 UTF-8 JSON
JWT Bearer Token 鉴权放入请求 Header
RESTful 规范：GET 查询、POST 新增、PUT 修改、DELETE 删除
11.2 统一返回体格式
json
{
    "code": 0,
    "message": "success",
    "data": {},
    "timestamp": "2026-07-19T10:30:00",
    "requestId": "随机追踪ID"
}
code=0 代表成功，非 0 为业务错误码。
11.3 业务错误码节选
表格
错误码	含义
0	成功
1001	Token 无效 / 过期
2001	文件格式不支持
3001	OCR 识别失败
3002	Parser 解析异常
4000	数据不存在
5000	导出任务失败
9000	系统未知异常
11.4 核心接口清单
鉴权
POST /api/v1/auth/login 登录获取双 Token
GET /api/v1/auth/me 获取当前登录用户信息
上传任务
POST /api/v1/upload/file 单文件上传
POST /api/v1/upload/files 多文件
POST /api/v1/upload/folder 前端目录上传
GET /api/v1/task/{task_id} 查询任务进度
报告与指标
GET /api/v1/report/list 分页报告列表
GET /api/v1/report/{id} 报告详情
GET /api/v1/result/list 指标筛选查询
GET /api/v1/result/trend 指标时序趋势
预览定位
GET /api/v1/report/{id}/preview 获取页面预览地址
GET /api/v1/result/{rid}/location 获取指标 bbox 坐标
导出
POST /api/v1/export 异步生成 Excel/CSV 下载链接
后台管理接口
/api/v1/hospital 医院 CRUD
/api/v1/dictionary 标准指标维护
/api/v1/config 系统配置
/api/v1/review 人工审核
/api/v1/audit 审计日志
11.5 安全约束
接口限流、CORS 跨域、参数自动校验、防越权查询。
11.6 接口文档
FastAPI 自动生成 /docs Swagger 在线调试文档。
第十二章 前端系统设计
12.1 技术栈
Vue3 + TypeScript + Vite + Element Plus + Pinia + Axios + ECharts + PDF.js
12.2 前端目录结构
text
src/
├── api/          后端请求封装
├── components/   通用业务组件
├── layouts/      后台布局
├── router/       路由守卫
├── store/        Pinia状态管理
├── styles/       全局样式
├── utils/        工具函数
└── views/        页面视图
12.3 核心页面
登录页
Dashboard 首页（系统统计看板）
上传中心（拖拽 / 目录上传）
任务列表（查看解析进度、重试）
报告管理列表
报告详情（左右分栏：PDF 预览 + 指标表格）
人工审核页面
趋势分析图表页
后台配置（医院、指标字典、系统参数）
日志审计页面
12.4 核心交互：指标双向定位
前端调用坐标接口获取页面与 bbox，PDF 画布绘制高亮框，自动滚动定位指标。
12.5 前端性能优化
路由懒加载、表格虚拟滚动、图片懒加载、接口缓存、WebSocket 推送任务进度。
12.6 权限控制
路由守卫拦截无权限访问，按钮粒度区分角色权限。
第十三章 Parser 解析引擎详细设计
13.1 定位
OCR/PDF 文本仅提供原始文字，Parser 是提取检验字段核心业务引擎。
13.2 三层架构
总调度引擎 → 表头解析 → 表格行解析 → 字段匹配器
13.3 内部组件
ParserEngine、HeaderParser、TableParser、FieldMatcher、RuleEngine
13.4 表格识别逻辑
依靠文字块坐标横向 / 纵向间距区分表格行列，兼容无边框电子化验单。
13.5 单条指标输出字段
项目名称、缩写、结果、单位、参考区间、异常标记，绑定页面坐标溯源。
13.6 多页合并
长报告分页数据自动合并，自动过滤重复项目。
13.7 容错处理
单行识别失败不中断整份报告，自动送入人工审核队列。
13.8 性能优化
正则预编译、医院模板内存缓存、多页面并行解析。
13.9 V4.0 扩展
接入 LLM 视觉大模型，识别不规则非标化验单、自动修正 OCR 错误。
第十四章 文件存储与缓存设计
14.1 分层存储目录
text
storage/
├── upload/original  原始文件永久存储
├── preview           PDF预览图片
├── thumbnail         缩略图
├── ocr/cache         OCR识别缓存
├── cache/pdf         PDF渲染缓存
├── export            导出临时文件（7天自动清理）
└── archive           归档文件
14.2 文件命名规则
原始文件使用 UUID 命名防重，数据库保存原文件名、MD5、SHA256、存储路径。
14.3 文件去重机制
上传计算 SHA256 哈希，数据库存在相同哈希直接复用文件，不重复写入磁盘。
14.4 StorageManager 统一接口
读写、缩略图、归档全部封装，后续切换 MinIO/OSS 仅替换适配器，业务代码无修改。
14.5 缓存生命周期
OCR、PDF 渲染缓存永久保留；导出文件 7 天自动清理。
14.6 文件下载安全
禁止直接暴露物理路径，所有下载走鉴权 API，校验用户数据权限。
14.7 扩展规划
支持 S3/MinIO 对象存储，适配云端集群部署。
第十五章 人工审核（Review）系统设计
15.1 适用场景
OCR 低置信、未知项目、单位识别失败、参考区间异常、Parser 识别失败数据。
15.2 审核状态流转
Pending 待审核 → Assigned 已分配 → Reviewing 审核中 → Approved 通过 / Rejected 驳回 / ReParse 重解析
15.3 页面布局
左侧 PDF 原图预览，右侧指标编辑表格；点击表格自动定位原图对应文字。
15.4 修改留痕
修改项目 / 结果 / 单位后自动写入 audit_log，保存修改前后对比，支持历史回滚。
15.5 批量操作
批量通过、批量驳回、批量重新解析，提升大批量校对效率。
15.6 自动审核规则
OCR 置信度 > 0.99、字段完整无异常自动跳过人工审核。
15.7 审核统计看板
待审核数量、人均工作量、OCR 错误分类统计。
第十六章 系统安全设计
16.1 身份认证
JWT Access+Refresh 双令牌，登录失败 5 次锁定 30 分钟。
16.2 RBAC 权限模型
四级角色：Admin、Doctor、User、Viewer，菜单、按钮、数据范围分级隔离。
16.3 数据隐私
默认不采集患者身份证、姓名等敏感信息；如需存储采用 AES 字段加密，日志脱敏。
16.4 Web 安全
生产强制 HTTPS，ORM 参数化防 SQL 注入，前端输出 HTML 转义防 XSS。
16.5 文件上传安全
黑名单拦截 exe/bat/sh 等可执行文件，校验文件哈希防篡改。
16.6 审计日志
所有新增、修改、删除、配置变更永久留存，不可删除。
16.7 数据库安全
生产禁用 root 账号，分配最小权限账号，开启 SSL、慢查询日志。
16.8 配置安全
数据库密码、API 密钥存入.env，禁止提交 Git 仓库。
第十七章 性能优化设计
17.1 异步任务分离
OCR、PDF 渲染、Excel 导出全部 Celery 异步，不阻塞 Web 接口。
17.2 多线程池隔离
PDF 渲染、OCR、解析使用独立线程池，资源互不抢占。
17.3 多级缓存
磁盘缓存（OCR/PDF 渲染）、Redis 缓存（指标字典、系统配置、热点查询）。
17.4 数据库优化
合理索引、批量插入代替循环单条写入、大数据分页、历史数据分区归档。
17.5 OCR 专项优化
缓存重复图片识别结果、大图自动缩放、文字 PDF 跳过 OCR。
17.6 前端优化
虚拟表格、图片懒加载、按需渲染 PDF 页面、ECharts 数据缓存。
17.7 量化性能指标
单页 OCR≤0.8s；单份解析≤5s；列表查询≤300ms；万条 Excel 导出≤10s。
第十八章 部署架构设计
18.1 单机开发架构
Nginx + Vue 前端 + FastAPI + MySQL + Redis 本地运行。
18.2 Docker Compose 生产架构
容器列表：nginx、frontend、backend、mysql、redis，一键启停。
18.3 硬件推荐
开发机：8 核 16G SSD
小型业务：8 核 32G SSD
中型批量解析：16 核 64G + GPU（可选）
18.4 Nginx 职责
HTTPS 反向代理、静态资源缓存、Gzip 压缩、接口限流。
18.5 微服务扩展规划 (V4.0)
OCR、Parser 拆分为独立微服务，K8s 水平扩容大批量任务。
18.6 监控配套
Prometheus+Grafana 采集系统、业务指标，支持钉钉 / 企业微信告警。
18.7 备份策略
MySQL 每日全量 + 实时 binlog；上传文件每周备份，定期恢复演练。
第十九章 AI 扩展设计（V4.0 规划）
19.1 设计原则
现有规则解析为主，LLM 视觉模型仅做增强，不替代原有 Parser。
19.2 AI 网关统一适配层
兼容火山方舟、OpenAI、本地私有大模型，插件化切换无需修改业务代码。
19.3 规划 AI 能力
视觉 LLM 直接解析化验单图片 / PDF，修正 OCR 识别错误
自动识别新医院版式，生成模板配置文件
RAG 医学知识库，指标临床辅助解读
自然语言查询指标历史数据
AI 自动预审识别异常数据
Agent 自动完成批量解析、报表导出
19.4 Model 适配器架构
BaseLLM 抽象基类，各厂商 API 独立插件实现。
19.5 AI 安全约束
AI 输出仅为辅助建议，高危异常必须人工复核；敏感数据自动脱敏后传入模型。
19.6 Token 成本优化
热点查询、重复化验单 LLM 结果 Redis 缓存，减少计费消耗。
19.7 版本路线
V4.0：RAG、智能审核、趋势 AI 分析
V4.1：自然语言查询、Prompt 管理
V4.2：Agent、MCP 工具调用
V5.0：多智能体协同、全自动模板学习
第二十章 系统测试设计
20.1 测试分层
单元测试 → 集成测试 → 系统 E2E → 性能压力测试
20.2 单元测试覆盖模块
OCR、PDF、Parser、Normalize、文件工具、API 逻辑，覆盖率≥90%。
20.3 功能测试用例覆盖
文字 PDF、扫描 PDF、手机拍照、多医院模板、异常指标、导出、预览定位。
20.4 性能测试
千份文件批量压力、高并发上传、十万条导出耗时测试。
20.5 安全测试
SQL 注入、XSS、越权访问、上传漏洞、暴力登录拦截。
20.6 验收标准
OCR 识别率≥98%；指标识别≥99%；全年可用率≥99.9%；核心缺陷清零方可上线。
20.7 自动化测试
pytest 后端单元、Playwright 前端 UI 自动化，CI 提交自动执行回归。
第二十一章 运维与监控设计
21.1 监控指标
CPU、内存、磁盘、GPU、MySQL、Redis、OCR 成功率、任务队列堆积。
21.2 分级告警
P0 致命 5 分钟响应；P1 严重 30 分钟；P2 普通工作时段处理；通知渠道：钉钉 / 企业微信 / 邮件。
21.3 健康检查接口
GET /api/v1/system/health 输出全服务运行状态，供容器存活探测。
21.4 配置中心
.env + 数据库双配置，支持在线热更新、版本回滚、操作审计。
21.5 发布回滚
镜像版本化，数据库脚本版本管理，支持一键回滚应用与数据。
21.6 备份与容灾
每日 MySQL 全量、实时 binlog；文件定期备份；RTO≤4 小时，RPO≤1 小时。
21.7 运维 SLA
系统年可用率≥99.9%，API 成功率≥99.9%。
21.8 运维规范
发布审批、定期巡检、故障 RCA 复盘、标准化操作 SOP。
第二十二章 项目实施与版本规划
22.1 开发阶段划分
M1：基础框架、用户、数据库搭建
M2：上传、OCR、PDF 解析
M3：Parser、标准化、预览
M4：人工审核、导出、趋势
M5：性能优化、全量测试、上线部署
22.2 项目组织架构
项目经理、产品、后端、前端、OCR/AI 开发、测试、运维、实施工程师
22.3 新增医院实施流程
收集多类型化验单样本 → 版式分析 → 编写 Parser 插件 + yaml 配置 → 单元测试 → 上线监控
22.4 风险管控
OCR 识别模糊、新医院版式、大批量性能、人员变动建立文档兜底。
22.5 交付物清单
前后端完整源码
Docker Compose 部署脚本
MySQL 初始化 / 升级 SQL
全套设计文档（本文档）
用户操作手册、运维手册
全套测试报告
22.6 长期扩展方向
医疗对接：HIS/LIS/EMR/FHIR
AI 智能化：报告解读、自动模板
SaaS 多租户集团平台
云原生 K8s 集群
附录
附录 A 术语对照表
表格
缩写	全称	释义
OCR	Optical Character Recognition	光学文字识别
LLM	Large Language Model	大语言模型
RAG	Retrieval-Augmented Generation	检索增强生成
RBAC	Role-Based Access Control	基于角色权限
JWT	JSON Web Token	登录鉴权令牌
SDS	软件详细设计说明书	
SLA	服务等级协议	
RTO/RPO	灾难恢复指标	
附录 B 标准映射示例
检验项目
谷丙转氨酶：ALT、GPT、谷丙
谷草转氨酶：AST、GOT
单位
IU/L → U/L
umol/L/ μmol/L 统一 μmol/L
附录 C 代码规范
Python：PEP8，Black 格式化；Vue3：ESLint+Prettier；Git 采用 Git Flow 分支管理。
附录 D 数据库版本管理
sql 目录按版本文件夹存放初始化 / 升级脚本，配套回滚 SQL。
文档结束