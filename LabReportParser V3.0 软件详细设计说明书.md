# LabReportParser V3.0 软件详细设计说明书

文档编号：LRP-SDS-V3.0

版本：V3.0.0

创建日期：2026-07-14

文档格式：Markdown

字符编码：UTF-8

## 目录

001 修订记录

002 文档目录

第 1 章 项目概述

第 2 章 需求分析

第 3 章 系统总体架构

第 4 章 技术选型与设计原则

第 5 章 系统模块详细设计

第 6 章 OCR 与文档解析引擎设计

第 7 章 MySQL 数据库完整设计

第 8 章 文件存储体系设计

第 9 章 REST API 与 WebSocket 接口设计

第 10 章 前端页面详细设计

第 11 章 后端服务核心设计

第 12 章 在线预览与指标定位功能设计

第 13 章 人工复核、审计日志设计

第 14 章 趋势分析与数据导出模块设计

第 15 章 系统配置、模板与权限管理设计

第 16 章 部署架构（Docker Compose）

第 17 章 测试方案设计

第 18 章 运维设计

第 19 章 产品迭代 Roadmap

第 20 章 系统安全设计

第 21 章 性能优化设计

第 22 章 项目实施与版本规划

附录 A MySQL 完整建表 SQL

附录 B 系统核心配置文件模板

附录 C 错误码全集定义

附录 D OpenAPI 规范说明

附录 E 缩略词对照表

附录 F 术语对照表

附录 G 代码规范

附录 H 数据库版本管理

***

# 001 修订记录

## 文档基础信息

表格

| 项目   | 内容                                   |
| ---- | ------------------------------------ |
| 项目名称 | LabReportParser V3.0 医学检验报告智能解析与管理平台 |
| 文档名称 | 软件详细设计说明书                            |
正在解析13个未解析报告）| 文档编号 | LRP-SDS-V3.0                         |
| 当前版本 | V3.0.6                               |
| 文档状态 | 编写中                                  |
| 创建时间 | 2026-07-14                           |
| 字符编码 | UTF-8                                |

## 修订规则

1. 版本格式：主版本。次版本。修订号，如 V3.0.0；
2. 每次修改必须记录修改日期、版本、修改章节、修改原因、修改内容、修改人；
3. 禁止覆盖历史记录，所有变更永久留存；
4. 修改分级：A 架构修改、B 功能新增 / 调整、C 文案补充修正、D 格式排版调整。

## 版本历史记录

表格

| 日期         | 版本     | 等级 | 修改章节 | 修改内容                              | 修改人   |
| ---------- | ------ | -- | ---- | --------------------------------- | ----- |
| 2026-07-14 | V3.0.0 | A  | 全文   | 完成完整设计文档初稿，整合全部需求、架构、数据库、前后端、部署方案 | 设计工程师 |
| 2026-07-16 | V3.0.1 | B  | 2.1.3、2.1.7、10.6、10.8、11.1 | 补充医院解析器两种实现方式、重新识别功能、PDF预览实现、前端API调用规范、医院模板学习特征细节 | 设计工程师 |
| 2026-07-19 | V3.0.2 | B  | 2、4、5、6、9、10、11 | 新增AI识别功能模块，包括大模型调用、配置管理、降级机制、API接口、前端页面设计 | 设计工程师 |
| 2026-07-19 | V3.0.3 | B  | 5.1 | 补充重复文件处理逻辑、多文件上传处理逻辑、同步/异步处理机制说明 | 设计工程师 |
| 2026-07-19 | V3.0.4 | B  | 10.6 | 补充重新识别功能中解析引擎下拉框选择功能说明 | 设计工程师 |
| 2026-07-22 | V3.0.5 | B  | 13.4、13.5、14.4、14.5、14.6、14.7 | 补充报告审核页面状态保持与表格样式设计、趋势分析多指标查询与交互功能设计、数据库连接锁优化 | 设计工程师 |
| 2026-07-23 | V3.0.6 | B  | 6.2、6.5-6.7、7.5-7.6、8.4、13.6、18.5-18.7、19.4、第20-22章、附录F-H | 补充OCR后处理规则与配置示例、页面判定算法、PDF异常兼容、模板版本管理、数据分区规划、备份策略、StorageManager接口、审核统计看板、分级告警、健康检查、发布回滚、AI视觉LLM与Agent规划、系统安全设计、性能优化设计、项目实施规划、术语对照表、代码规范、数据库版本管理 | 设计工程师 |

## 审核 / 批准记录

表格

| 审核日期 | 审核人 | 审核结果 | 备注 |
| ---- | --- | ---- | -- |
| 待审核  | -   | -    | -  |
| 批准日期 | 批准人 | 审批结果 | 备注 |
| 待批准  | -   | -    | -  |

## 文档状态定义

草稿、编写中、待评审、已评审、已批准、已发布、已废弃，当前状态：编写中。

## 配套文档统一版本约束

需求规格说明书、数据库设计文档、API 文档、测试手册、部署手册全部统一为 V3.0 版本。

## 变更流程

变更申请 → 架构影响分析 → 技术评审 → 文档修改 → 二次评审 → 版本批准 → 新版发布

***

# 002 文档目录

见文档顶部一级目录，全文档采用三级章节编号（1 / 1.1 / 1.1.1）；图表独立编号：图 X-X、表 X-X；数据库表编号 DB-XX；接口编号 API-XX。

***

# 第 1 章 项目概述

## 1.1 项目基础信息

项目全称：LabReportParser V3.0 医学检验报告智能解析与管理平台

简称：LRP

英文全称：Laboratory Report Intelligent Parsing and Management Platform

## 1.2 项目定位

企业级本地部署检验报告解析管理平台，支持 PDF / 图片化验单批量导入、自动 OCR 识别、多医院报告统一标准化、数据入库、溯源预览、指标趋势分析、人工校对。采用前后端分离架构，MySQL 持久化存储，支持目录批量上传，满足个人健康档案、科研数据采集、医疗机构报告整理场景。

## 1.3 建设背景

1. 各医院化验单格式、指标命名、单位、排版不统一，人工录入效率低、误差大；
2. 报告分为文字 PDF、扫描 PDF、手机拍照图片，缺乏统一自动化解析工具；
3. 用户需要统一汇总多时段、多医院检验数据，生成趋势对比；
4. 原始文件与解析数据脱节，无法快速根据指标定位化验单原图；
5. 原有单脚本工具无法支持批量任务、权限管理、异步处理、多人协作。

## 1.4 建设目标

1. 兼容 PDF、主流图片格式，支持单文件、多文件、文件夹、拖拽四种上传模式；
2. 自动分流文字 PDF / 扫描件 / 图片，内置 PaddleOCR 完成图像文字识别；
3. 插件化医院解析模板，自动识别医院并提取患者、采样、检验指标原始数据；
4. 内置标准检验项目库，自动统一指标名称、缩写、英文、单位、参考区间；
5. 全链路溯源：每条指标绑定源文件、页码、OCR 坐标，点击指标自动高亮原图对应区域；
6. MySQL 存储完整业务数据，开发环境数据库账号 root、密码 root；
7. 异步任务队列处理大批量解析，支持任务监控、失败重试；
8. 提供在线预览、全文检索、单指标时序趋势图、Excel/CSV 批量导出；
9. 人工校对模块，所有修改留存审计日志，支持数据回滚；
10. RBAC 权限管理、Docker 一键容器化部署、完整运维备份方案。

## 1.5 设计原则

1. 高内聚低耦合：各模块职责独立，通过接口通信；
2. 配置化无硬编码：医院模板、指标字典、系统参数全部通过配置 / 数据库维护；
3. 插件扩展：新增医院解析规则仅增加配置文件，不改动核心代码；
4. 全链路可追溯：所有解析数据绑定原始文件、页面、坐标、操作记录；
5. 异步解耦：OCR、缩略图生成、批量导出全部异步执行，不阻塞前端请求；
6. 安全可控：接口鉴权、文件访问权限校验、密码加密存储、上传文件过滤。

## 1.6 系统业务范围

### 系统实现功能

文件上传、OCR 图像预处理、PDF 文本提取、医院模板识别、指标标准化、MySQL 入库、文件缓存、在线预览、指标定位高亮、人工复核、审计日志、全文检索、趋势图表、数据导出、用户权限、系统配置、异步任务管理、容器化部署、数据自动备份。系统不实现功能

1. 不生成医疗诊断建议，所有分析仅作为数据整理展示，页面强制增加免责声明；
2. 不对接医院 HIS/LIS/EMR 院内系统，仅解析用户本地上传文件；
3. V3.0 不支持多租户、FHIR/HL7 医疗接口、线上实时拍照识别；
4. 无第三方云存储，所有文件本地服务器磁盘存储。

## 1.7 使用角色

1. 普通个人用户：上传个人化验单、查看指标、导出健康数据、查看趋势；
2. 科研人员：批量导入报告、筛选异常指标、批量导出数据集；
3. 医护人员：查看患者历次检验趋势、人工修正 OCR 识别错误；
4. 数据管理员：维护标准指标库、医院解析模板、审计操作记录；
5. 系统管理员：用户权限分配、系统参数配置、数据备份、运维监控。

## 1.8 核心业务总流程

用户上传文件 / 文件夹 → 后端校验文件、分片存储原始文件 → 创建异步解析任务存入 Celery 队列

→ 文件分流：文字 PDF 直接提取文本，扫描 PDF / 图片执行 OCR 预处理 + 文字识别

→ 生成页面缓存图、OCR 识别 JSON 缓存，避免重复识别

→ 匹配医院解析模板，提取患者信息、采样时间、原始检验指标

→ 标准化服务统一指标名称、单位、参考范围，AI 辅助修正 OCR 文字错误

→ 计算报告数据质量分，低于 85 分标记待人工复核

→ 全部数据分层写入 MySQL 关联数据表

→ 前端展示报告列表、指标明细，点击指标自动预览原图并高亮对应坐标区域

→ 支持人工修改指标数据，操作写入审计日志

→ 多维度检索、生成指标趋势折线图、导出 Excel/CSV 文件

→ 管理员维护模板、用户、系统配置，定时自动备份数据库与文件

## 1.9 名词解释

表格

| 名词         | 释义                                       |
| ---------- | ---------------------------------------- |
| OCR        | 光学字符识别，将图像文字转换为可编辑文本                     |
| Report     | 单份完整化验单文档，唯一标识 report\_id                |
| ReportPage | 单份报告内的一页 PDF / 单张图片                      |
| Bbox       | OCR 文本块坐标，left/top/right/bottom，用于指标定位高亮 |
| 标准化映射      | 将不同医院别名统一为标准检验项目（GPT / 谷丙转氨酶 = ALT）      |
| Celery     | Python 异步任务队列，处理耗时 OCR、解析任务              |
| RBAC       | 基于角色的权限控制模型                              |
| 审计日志       | 记录所有人工修改、系统关键操作，永久不可删除                   |
| 缩略图 / 预览缓存 | 预生成页面图片，加速前端预览加载                         |

***

# 第 2 章 需求分析

## 2.1 功能性需求

### 2.1.1 文件上传模块需求

1. 支持格式

   PDF：原生文字 PDF、扫描版 PDF；图片：jpg/jpeg/png/bmp/tif/tiff/webp；
2. 四种上传模式
   - 整目录上传：递归读取子目录全部合规文件；
   - 单文件上传；
   - 多选多文件批量上传；
   - 页面拖拽上传；
3. 上传辅助能力
   - 大文件分片断点续传；
   - MD5 校验去重，重复文件跳过解析；
   - 自动过滤损坏 / 空文件；
   - WebSocket 实时推送上传、解析进度；
   - 每条上传生成独立 UploadTask 任务记录；
   - 上传有进度条，解析也要有进度条
4. 自动采集文件元数据：文件名、完整存储路径、文件类型、大小、MD5、上传时间。

### 2.1.2 OCR 与文档解析需求

1. PDF 分流逻辑：文字 PDF 调用 pdfplumber/PyMuPDF 提取表格文本，无需 OCR；扫描 PDF 每页转图送入 OCR；
2. 图片预处理流水线：灰度化、降噪、自动旋转矫正、图像增强；
3. OCR 输出：文本内容、识别置信度、文本块四坐标 bbox；
4. 缓存机制：每页预览图、OCR 识别 JSON 持久缓存，重复上传直接读取缓存；
5. 数据质量评分 quality\_score（0-100），依据 OCR 置信度、指标完整度，低于 85 标记待复核。

### 2.1.3 医院模板识别需求

1. 自动匹配医院专用解析插件，无匹配则使用通用解析器 CommonParser；
2. 后台可视化新增 / 编辑医院模板，人工标记后系统自动学习报告特征；
3. 模板基于 YAML 配置文件，新增医院无需修改后端核心代码；
4. 解析代码默认值：新增医院时，parser_code 字段默认值为 "common"，使用通用解析器；
5. 医院解析器两种实现方式：
   - OCR 坐标解析：适用于扫描版 PDF，通过 OCR 识别的文本块坐标（bbox）来定位和提取表格数据，支持单列、双列等多种布局；
   - 纯文本解析：适用于文字可提取 PDF，直接通过文本行匹配和正则表达式提取数据；
6. 模板学习特征：系统自动学习报告特征包括但不限于：医院名称关键字、报告页眉页脚特征、表格列名特征、参考区间格式特征、指标命名规律等。

### 2.1.4 检验指标标准化需求

1. 原始提取字段：项目名、数值、单位、参考区间、异常标记、患者信息、采样时间；
2. 标准化映射：别名、缩写、中英文统一匹配标准指标库；统一单位、标准化参考区间格式；
3. AI 纠错：大模型修正 OCR 识别错字（AIT→ALT、谷内转氨酶→谷丙转氨酶）；
4. 强制溯源字段：source\_file、source\_path、file\_type、page\_no、bbox 坐标组。

### 2.1.5 数据入库存储需求

1. 数据库采用 MySQL8.0，开发环境连接配置：host=[localhost](https://link.wtturl.cn/?target=https%3A%2F%2Flocalhost\&scene=im\&aid=497858\&lang=zh "autolink"),port=3306,db=LabReport,user=root,password=root；生产环境禁止明文配置账号密码，使用环境变量注入；
2. 分层关联入库：上传任务表、报告主表、报告页表、OCR 块表、检验结果表、医院表、标准指标表；
3. 文件分层物理存储：原始文件目录、预览缓存目录、缩略图目录、OCR JSON 缓存目录；
4. 全局唯一 report\_id 关联所有子数据，业务数据软删除，物理文件支持定时清理。

### 2.1.6 在线预览与指标定位核心需求

1. 前端双栏布局：左侧指标列表，右侧预览画布；
2. 点击任意指标，右侧自动切换对应页面，渲染红色高亮框显示指标 bbox 区域；
3. PDF 使用 PDF.js 预览，直接预览上传后的原始 PDF 文件，而非解析过程中生成的图片；图片支持缩放、拖拽、翻页、全屏查看；
4. Excel 导出增加预览超链接，点击直接跳转网页定位化验单。

### 2.1.7 人工复核与审计需求

1. 指标全字段编辑：标准名称、数值、单位、参考区间均可手动修改；
2. 复核状态枚举：未复核、已复核、人工修改；
3. 审计日志完整记录：操作人、操作时间、原始值、修改后值、关联 report\_id；
4. 支持单条指标回滚至原始 OCR 识别数据。
5. 审核报告详细页面为：上面显示该报告综合信息：如报告文件名、报告文件名称、报告样品采集时间、报告的医院等报告的表头信息；左侧显示该报告的化验信息，如指标名称、化验结果、单位、参考区间、是否正常（高用向上箭头，低用向下箭头等）；右侧显示该化验单的预览。左侧表格可以编辑功能（更增删改和保存）；右侧有“重新识别”按钮，点该按钮重新识别化验单项目，并删除左侧表格内容，将重新识别的内容填进去。

### 2.1.8 检索与趋势分析需求

1. 全文模糊检索：医院、患者姓名、指标中文名 / 缩写、报告时间、文件名；
2. 多维度筛选：时间区间、医院、指标、异常状态、文件类型；
3. 时序趋势图：单指标历次检测折线图，自动标注上升 / 下降 / 平稳；
4. 自动汇总长期连续异常指标清单，图表支持导出图片。

### 2.1.9 数据导出需求

支持 Excel、CSV 批量导出；导出字段包含医院、报告时间、患者信息、标准指标、结果、单位、参考范围、异常标记、源文件、页码、预览链接、置信度、复核状态。

### 2.1.10 用户权限管理（RBAC）

预设角色：普通用户、医护人员、数据管理员、系统管理员；权限粒度：报告查看、编辑、批量上传、模板管理、用户管理、系统配置、数据导出；支持新增 / 禁用用户、重置密码、角色分配。

### 2.1.11 系统配置与异步任务

1. 配置中心：OCR 线程数、缓存过期周期、自动备份周期、文件存储路径、质量分阈值；
2. 异步队列 Celery+Redis：OCR、解析、缩略图、批量导出全部异步；
3. 任务监控：查看队列、终止任务、失败文件一键重解析。

### 2.1.12 AI 识别功能需求

1. 大模型集成：调用火山方舟大模型 API，直接利用 AI 能力提取化验单数据；
2. 多模态输入：支持文本+图片模式，PDF 转换为图片后作为多模态输入；
3. 配置管理：支持配置并保存 API Key、base_url、模型名，配置持久化存储；
4. 提示词管理：提供默认提示词，支持自定义修改提示词内容；
5. 降级机制：当大模型不可用（模型未开通、API Key 无效、网络异常等）时，自动降级到传统医院解析器；
6. 识别结果：返回患者信息、检验指标、识别状态（AI 使用 / 降级原因）；
7. 数据入库：AI 识别结果与传统解析结果统一入库逻辑，保证数据一致性；
8. 前端页面：提供 AI 识别专属页面，支持文件上传、配置管理、识别结果展示。

## 2.2 非功能性需求

### 2.2.1 性能需求

1. 单页文件解析（含 OCR）≤3s；支持 20 文件并发解析无阻塞；
2. 前端列表、预览页面加载响应≤500ms；缓存读取解析速度提升 90%；
3. 千条指标 Excel 导出耗时≤10s。

### 2.2.2 兼容性需求

前端兼容 Chrome、Edge、Firefox、Safari 最新稳定版；后端基于 Docker 容器统一依赖；兼容手机拍照模糊化验单、常规扫描件。

### 2.2.3 可扩展性

插件化解析模板，新增医院仅配置文件；模块完全解耦；API 预留扩展字段，后续支持小程序、APP 对接；指标库动态更新无需重启服务。

### 2.2.4 可追溯性

任意指标完整溯源原始文件、页面坐标、OCR 原文、全部修改记录；审计日志永久留存不可删除；数据库外键完整关联所有业务实体。

### 2.2.5 安全性

密码加密存储；接口 Token 鉴权、越权拦截；文件访问鉴权；严格过滤上传文件类型，拦截可执行脚本；生产环境数据库密码不落地明文。

### 2.2.6 可运维性

Docker Compose 一键部署全套服务；全链路日志归档；MySQL 定时自动备份；缓存文件自动清理；服务健康监控接口支持告警对接。

## 2.3 外部接口需求

1. RESTful API：上传、任务、报告 CRUD、指标查询、预览、导出、趋势、用户权限；
2. WebSocket：实时推送上传、解析进度；
3. 内部服务接口：OCR 识别服务、标准化服务、导出服务；
4. 数据存储接口：MySQL 持久化、Redis 缓存与任务队列。

## 2.4 需求边界

V3.0 不支持院内系统对接、多租户、FHIR 医疗标准、在线实时拍照；所有健康分析仅为数据整理，不具备诊断效力。

***

# 第 3 章 系统总体架构

系统采用 **前后端分离四层架构** ，整体分为前端表现层、后端 API 服务层、核心业务逻辑层、数据持久存储层，搭配中间件 Redis、Celery 处理异步任务。

## 3.1 架构分层说明

### 3.1.1 前端表现层

技术栈 Vue3+TS，负责页面交互、文件上传、在线预览、指标可视化、趋势图表渲染，通过 Axios 调用后端 REST API，WebSocket 接收实时任务进度。

### 3.1.2 API 网关层（Nginx）

统一反向代理，分发前端静态资源、后端 API 请求，处理跨域、HTTPS 加密、请求限流、静态缓存。

### 3.1.3 后端核心服务层（FastAPI）

划分子模块：上传服务、解析调度服务、标准化服务、预览服务、趋势分析服务、导出服务、用户权限服务、配置管理服务；对外统一输出 REST 接口，耗时任务提交至 Celery 队列异步执行。

### 3.1.4 异步任务中间件层

Redis：缓存、任务队列存储；Celery Worker：消费 OCR、PDF 解析、缩略图生成、批量导出任务。

### 3.1.5 数据存储层

1. MySQL8.0：结构化业务数据（报告、指标、用户、模板、审计日志）；
2. 本地磁盘文件存储：原始 PDF / 图片、预览缓存图、缩略图、OCR 识别 JSON；
3. Redis 缓存：临时会话、任务进度、高频查询指标缓存。

## 3.2 整体架构流程图

预览

查看代码

```
flowchart LR
    User[浏览器用户] --> Nginx[Nginx网关]
    Nginx --> Front[Vue3前端静态资源]
    Nginx --> FastAPI[FastAPI后端服务]
    FastAPI --> Redis[Redis中间件]
    FastAPI --> Celery[Celery任务调度]
    CeleryWorker[Celery Worker集群] --> OCR[PaddleOCR引擎]
    CeleryWorker --> PDFParser[PDF解析模块]
    CeleryWorker --> FileStorage[本地磁盘文件存储]
    FastAPI --> Mysql[MySQL8.0数据库]
    FastAPI --> StandardService[指标标准化服务]
    FastAPI --> PreviewService[预览定位服务]
    FastAPI --> ExportService[导出服务]
    FastAPI --> AuthService[权限鉴权服务]
```

![](//lf-flow-web-cdn.doubao.com/obj/flow-doubao/doubao/chat/static/image/logo-icon-white-bg.ac11b4b2.png)豆包

你的 AI 助手，助力每日工作学习

## 3.3 模块间通信规范

1. 前端 ↔ 后端：HTTP/HTTPS REST API、WebSocket；
2. 后端 ↔ Celery：Redis 消息队列；
3. 后端 ↔ 数据库：SQLAlchemy ORM；
4. 内部子模块之间：函数调用、统一数据实体类传输，不直接跨模块操作数据库。

## 3.4 部署架构分层

单服务器容器化部署，所有组件封装 Docker 容器：Nginx 容器、前端容器、FastAPI 后端容器、Celery Worker 容器、Redis 容器、MySQL 容器，docker-compose 统一编排启动。

***

# 第 4 章 技术选型与设计原则

## 4.1 前端技术栈

表格

| 组件        | 选型                | 用途            |
| --------- | ----------------- | ------------- |
| 基础框架      | Vue3 + TypeScript | 页面主体开发、类型约束   |
| UI 组件库    | Element Plus      | 表单、表格、弹窗、上传组件 |
| 状态管理      | Pinia             | 全局用户、任务状态存储   |
| 网络请求      | Axios             | 后端 API 调用     |
| PDF 预览    | PDF.js            | 在线解析渲染 PDF 文档 |
| 图表库       | ECharts           | 指标趋势折线图       |
| WebSocket | 原生 WebSocket      | 接收任务实时进度      |
| 构建工具      | Vite              | 前端打包编译        |

前端 API 调用规范：所有页面组件必须使用封装好的 `request` 工具（`utils/request.ts`）进行 API 调用，禁止直接使用原生 Axios；`request` 工具自动处理：添加 baseURL（`/api/v1`）、添加 Authorization Token、统一处理响应格式（提取 `response.data`）、统一错误处理。

## 4.2 后端技术栈

表格

| 组件     | 选型                   | 用途                  |
| ------ | -------------------- | ------------------- |
| Web 框架 | Python3.12 + FastAPI | 高性能异步 API 服务、自动接口文档 |
| ORM 框架 | SQLAlchemy 2.x       | MySQL 数据库对象映射       |
| 异步任务   | Celery + Redis       | OCR、解析、导出异步处理       |
| PDF 解析 | pdfplumber、PyMuPDF   | 文字 PDF 文本、表格提取      |
| 图像处理   | OpenCV-Python        | 图片预处理、矫正、降噪         |
| OCR 引擎 | PaddleOCR            | 扫描件、拍照图片文字识别        |
| 模糊匹配   | RapidFuzz            | 指标别名模糊匹配标准化库        |
| 数据处理   | Pandas               | Excel/CSV 导出、指标聚合统计 |
| AI 纠错  | LLM 接口封装             | OCR 错字智能修正          |
| AI 识别   | 火山方舟 API          | 大模型多模态识别化验单数据      |
| PDF转图片 | pdf2image + poppler  | PDF 转换为图片用于 AI 多模态输入 |

## 4.3 存储中间件选型

1. 数据库：MySQL8.0，稳定支持海量结构化检验数据，支持事务、外键关联；
2. 缓存 / 队列：Redis6，高性能内存存储，支撑任务队列与临时缓存；
3. 文件存储：服务器本地磁盘，分层目录管理原始文件与各类缓存资源。

## 4.4 部署技术

Docker、Docker Compose：容器化统一环境，一键启停整套服务；Nginx 反向代理、HTTPS 证书加密。

## 4.5 统一设计原则

1. 分层解耦：数据层、业务层、表现层完全隔离；
2. 配置优先：业务规则全部外置配置文件 / 数据库，无硬编码；
3. 异步化耗时操作：避免大文件解析阻塞 HTTP 请求；
4. 可追溯优先：所有业务数据绑定原始文件与坐标；
5. 权限最小化：不同角色仅开放对应操作接口；
6. 容错重试：解析失败自动重试，记录完整错误日志。

***

# 第 5 章 系统模块详细设计

## 5.1 上传中心模块

### 5.1.1 核心功能

四种上传模式、分片断点续传、文件校验去重、任务创建、元数据采集、进度推送。

### 5.1.2 内部子组件

文件校验器、分片存储工具、MD5 去重工具、任务创建器、WebSocket 进度推送器。

### 5.1.3 输入输出

输入：前端上传文件流 / 文件夹目录；输出：UploadTask 任务 ID，返回上传成功 / 失败文件列表。

### 5.1.4 重复文件处理逻辑

1. **MD5 计算**：文件上传完成后，计算文件 MD5 值；
2. **重复检测**：查询数据库中是否存在相同 MD5 且未删除的报告；
3. **处理策略**：
   - 当 `overwrite=false`（默认）：检测到重复时，删除刚上传的临时文件，返回 409 状态码，提示文件已存在，包含已有报告的 ID、文件名、创建时间；
   - 当 `overwrite=true`：检测到重复时，软删除已有报告及其关联数据（报告页、检验结果），删除旧文件，然后保存新文件；
4. **关联数据清理**：覆盖旧报告时，按以下顺序清理：
   - 删除该报告关联的所有 LabResult 记录；
   - 删除该报告关联的所有 ReportPage 记录；
   - 将原报告标记为已删除（is_delete=1）；
   - 删除原文件物理路径；

### 5.1.5 多文件上传处理逻辑

1. **任务创建**：接收多文件列表时，创建单个 UploadTask，file_total 为文件总数；
2. **逐文件处理**：循环处理每个文件，依次执行：
   - 生成唯一 report_id；
   - 保存文件到存储目录；
   - 计算 MD5；
   - 重复检测（同单文件逻辑）；
   - 保存报告记录到数据库；
   - 自动解析报告；
3. **进度更新**：每处理完一个文件，更新任务进度：`progress = (已处理数 / 文件总数) * 100`；
4. **结果汇总**：
   - 记录成功解析的报告 ID 列表；
   - 记录重复文件列表（包含已有报告信息）；
   - 最终返回任务 ID、成功数量、重复文件信息；

### 5.1.6 同步/异步处理机制

#### 5.1.6.1 当前实现（同步模式）

上传接口使用同步 `def`（非 `async def`），FastAPI 自动将其放入线程池执行，支持以下特性：

1. **多文件并行上传**：由于使用线程池，多个文件可以并行上传和解析，互不阻塞；
2. **单文件串行处理**：单个文件的上传、MD5 计算、重复检测、保存、解析流程串行执行；
3. **响应时机**：所有文件处理完成后返回响应，包含完整的处理结果；

#### 5.1.6.2 异步模式设计（预留）

大规模文件上传场景下，可切换为异步模式：

1. **上传阶段（同步）**：文件上传和保存同步完成；
2. **解析阶段（异步）**：解析任务提交至 Celery 队列，立即返回任务 ID；
3. **进度推送**：通过 WebSocket 实时推送每个文件的解析进度；
4. **结果查询**：前端通过任务 ID 查询最终解析结果；

#### 5.1.6.3 同步 vs 异步对比

| 特性 | 同步模式 | 异步模式 |
| ---- | ---- | ---- |
| 响应时间 | 等待所有文件解析完成 | 立即返回，不等待解析 |
| 并发能力 | 线程池并行（有限） | Celery Worker 集群（可扩展） |
| 进度感知 | 无实时进度 | WebSocket 实时推送 |
| 适用场景 | 文件数量少、解析快 | 文件数量多、解析耗时 |

### 5.1.7 上传接口参数说明

| 参数 | 类型 | 必填 | 说明 |
| ---- | ---- | ---- | ---- |
| file/files | UploadFile/List[UploadFile] | 是 | 单文件或多文件 |
| overwrite | bool | 否 | 是否覆盖重复文件，默认 false |

***

# 5.2 文档解析调度模块

### 5.2.1 核心功能

文件类型分流、分发异步任务、解析状态管理、失败任务重试。

### 5.2.2 处理逻辑

判断 PDF / 图片类型，提交对应解析任务至 Celery；监听任务执行状态，更新数据库任务进度。

## 5.3 OCR 图像处理模块

### 5.3.1 核心功能

图像预处理流水线、PaddleOCR 调用、识别结果结构化、bbox 坐标封装、OCR 缓存持久化。

### 5.3.2 输出实体

文本块数组：text、confidence、bbox 四坐标。

## 5.4 PDF 文本解析模块

### 5.4.1 核心功能

文字 PDF 页面遍历、表格提取、文本块坐标提取、无 OCR 直接输出结构化文本。

## 5.5 医院模板识别模块

### 5.5.1 核心功能

报告文本特征匹配、医院名称识别、加载对应解析插件、通用解析器兜底。

### 5.5.2 插件机制

所有医院解析器继承统一 Parser 基类，实现统一提取方法；新增医院仅新增 yaml 模板与解析子类。解析器支持两种实现方式：
- OCR 坐标解析：适用于扫描版 PDF，通过 OCR 识别的文本块坐标（bbox）来定位和提取表格数据，支持单列、双列等多种布局；
- 纯文本解析：适用于文字可提取 PDF，直接通过文本行匹配和正则表达式提取数据。
系统自动学习报告特征包括：医院名称关键字、报告页眉页脚特征、表格列名特征、参考区间格式特征、指标命名规律等。

### 5.5.3 模板版本管理

每个医院支持多版本解析模板，确保模板迭代时可回滚：

1. 每个医院在 `hospital_parser` 表中记录模板版本号、配置文件路径、发布时间；
2. 新版本模板发布后，旧版本保留 30 天可回滚期；
3. 模板变更通过版本号标记，解析时自动加载最新启用版本；
4. 支持为特定历史报告指定旧版模板重新解析；
5. 模板发布需经审核流程，记录变更原因、变更人、变更时间。

## 5.6 指标标准化模块

### 5.6.1 核心功能

原始指标匹配标准库、别名映射、单位统一、参考区间格式化、AI 错字修正、数据质量分计算。

### 5.6.2 依赖资源

数据库 LabItem 标准指标表、Alias 别名映射表、unit 单位字典配置。

## 5.7 文件缓存管理模块

### 5.7.1 核心功能

预览图生成、缩略图生成、OCR JSON 写入读取、缓存过期自动清理、目录统一管理。

## 5.8 在线预览与定位模块

### 5.8.1 核心功能

PDF / 图片资源加载、bbox 坐标渲染高亮、页面切换、缩放控制、预览链接生成。

## 5.9 人工复核与审计模块

### 5.9.1 核心功能

指标字段编辑、复核状态更新、审计日志全字段写入、数据回滚。

## 5.10 检索与趋势分析模块

### 5.10.1 核心功能

多条件联合查询、全文模糊检索、单指标时序数据聚合、ECharts 图表数据组装、异常指标统计。

## 5.11 数据导出模块

### 5.11.1 核心功能

Excel/CSV 文件生成、溯源字段拼接、预览超链接插入、文件流返回前端下载。

## 5.12 用户权限管理模块（RBAC）

### 5.12.1 核心功能

用户增删改查、角色分配、接口权限拦截、登录 Token 生成校验、密码加密存储。

## 5.13 系统配置模块

### 5.13.1 核心功能

OCR 参数、缓存周期、备份周期、文件路径、数据质量阈值可视化配置，配置持久化数据库。

## 5.14 日志与审计模块

### 5.14.1 核心功能

系统运行日志收集、操作审计日志写入、日志按日期归档、日志查询接口。

## 5.15 定时运维任务模块

### 5.15.1 核心功能

MySQL 定时备份、过期缓存清理、系统状态巡检、磁盘占用告警。

## 5.16 AI 识别模块

### 5.16.1 核心功能

大模型调用、PDF 转图片、AI 结果解析、降级机制、数据入库、配置管理。

### 5.16.2 处理流程

1. 文件上传 → 保存临时文件 → PDF 转图片（需 poppler）；
2. 读取 AI 配置（API Key、base_url、模型名、提示词）；
3. 调用火山方舟大模型 API，传入图片 + 提示词；
4. 解析大模型返回的 JSON 格式结果；
5. 标准化指标名称、单位、参考范围；
6. 写入数据库（与传统解析结果统一入库）；
7. 若大模型调用失败，自动降级到传统医院解析器。

### 5.16.3 降级机制

当以下情况发生时，自动降级到传统解析器：
- 大模型 API 调用失败（模型未开通、API Key 无效）；
- PDF 转图片失败（poppler 未安装）；
- 网络异常、超时；
- 大模型返回结果无法解析。

### 5.16.4 配置管理

AI 配置项包括：API Key、base_url、模型名称、默认提示词；配置持久化存储到数据库，支持动态修改。

***

# 第 6 章 OCR 与文档解析引擎设计

## 6.1 OCR 完整处理流程

原始图片 → OpenCV 图像读取 → 灰度化 → 高斯降噪 → 自动旋转矫正 → 对比度增强 → PaddleOCR 推理识别 → 结构化文本块（text/confidence/bbox）→ 写入 OCR 缓存 JSON → 返回解析模块。

## 6.2 PDF 分流处理逻辑

1. 文字原生 PDF：pdfplumber 提取每页文本表格，直接生成文本块，不调用 OCR；
2. 扫描 PDF：逐页转换为图片，送入 OCR 流水线处理；扫描 PDF 解析采用坐标表格提取方法，通过 OCR 识别的文本块坐标（bbox）进行行分组和列对齐，支持单列、双列等多种布局。

### 6.2.1 页面类型判定算法

通过综合评分判定每页 PDF 的类型，得分阈值可配置：

| 页面类型 | 判定规则 | 处理方式 |
|---------|---------|---------|
| TEXT 文字页 | 页面文字总长度 > 100，且可直接复制 | pdfplumber 直接提取文本，跳过 OCR |
| IMAGE 扫描页 | 文字极少，整页为图片 | 逐页转图片 → OCR 流水线处理 |
| MIXED 混合页 | 同时存在文字 + 图片 | 双数据源合并解析 |

判定综合评分 = 文字长度权重 × 文字量 + 图片面积权重 × 图片占比 + 字体数量权重 × 字体多样性。

### 6.2.2 PDF 异常兼容

- **加密 PDF**：检测到加密时给出提示，提示用户输入密码或使用未加密版本；
- **损坏 PDF**：捕获异常，标记该页为待人工处理，不阻塞其他页解析；
- **超大分页 PDF**：单页超过 50MB 时自动压缩分辨率后处理。

## 6.3 数据质量分计算规则

quality\_score = 平均 OCR 置信度*0.6 + 指标提取完整率*0.4；阈值 85，低于阈值标记 review\_status = 待复核。

## 6.4 OCR 缓存策略

以 report\_id+page\_no 作为缓存唯一 key，缓存文件存储路径 `storage/ocr/{report_id}/page_{page_no}.json`；重复解析同一页面直接读取缓存，跳过 OCR 推理。

## 6.5 OCR 后处理规则

OCR 识别结果可能存在形近字混淆、多余空格、换行断裂等问题，需经过后处理清洗：

1. **字符替换规则**（配置文件 `config/ocr_replace.yaml`）：
   - O→0（大写英文字母 O 替换为数字 0）
   - I→1（大写英文字母 I 替换为数字 1）
   - l→1（小写英文字母 l 替换为数字 1）
   - S→5（大写英文字母 S 替换为数字 5）
   - Z→2（大写英文字母 Z 替换为数字 2）
2. **空格清理**：清除多余空格、全角空格、不间断空格
3. **换行合并**：同一行文字因换行断裂时自动合并
4. **置信度过滤**：低于置信度阈值（默认 0.85）的文本块标记为低置信，供人工审核参考

## 6.6 OCR 配置示例

```yaml
ocr:
  engine: paddle
  language: chinese
  use_gpu: false
  thread_num: 8
  enable_cache: true
  confidence_threshold: 0.85
  auto_rotate: true
  auto_deskew: true
  replace_rules:
    'O': '0'
    'I': '1'
    'l': '1'
    'S': '5'
    'Z': '2'
```

## 6.7 OCR 性能优化手段

1. **缓存复用识别结果**：相同文件 SHA256 + 页码命中缓存时直接读取
2. **超大图片自动缩放**：超过 4000×4000 像素的图片自动等比缩放至合理分辨率
3. **文字 PDF 直接跳过 OCR**：经判定为文字 PDF 的页面直接提取文本
4. **批量任务异步处理**：大批量解析任务提交至后台线程，不阻塞前端请求
5. **多线程并行**：线程数可配置，推荐 CPU 核心数 × 2

## 6.8 AI 辅助纠错逻辑

将 OCR 原始文本送入轻量 LLM，传入标准指标字典作为约束，修正形近字、识别错字，返回修正后标准名称。

## 6.9 AI 识别流程与降级机制

### 6.9.1 AI 识别处理流程

1. PDF 转图片：使用 pdf2image + poppler 将 PDF 每页转换为图片；若 poppler 未安装，自动切换到纯文本模式；
2. 图片编码：将图片编码为 base64 格式，作为大模型 API 的多模态输入；
3. 大模型调用：调用火山方舟 API，传入图片数组和提示词；提示词包含标准化指标库作为约束，要求返回 JSON 格式结果；
4. 结果解析：解析大模型返回的 JSON，提取患者信息、检验指标列表；
5. 指标标准化：将 AI 识别的指标名称匹配标准指标库，统一单位和参考区间；
6. 数据入库：与传统解析结果统一逻辑写入数据库。

### 6.9.2 降级触发条件

当以下任一条件触发时，自动降级到传统医院解析器：
- 大模型 API 调用返回错误（404、401、500 等）；
- 模型未开通或不存在；
- API Key 无效或过期；
- PDF 转图片失败（poppler 未安装）；
- 网络超时或连接异常；
- 大模型返回结果格式无法解析（非 JSON）。

### 6.9.3 降级处理逻辑

降级时，系统自动执行以下流程：
1. 使用 pdfplumber/PyMuPDF 提取 PDF 文本；
2. 调用医院检测器识别报告所属医院；
3. 根据医院名称加载对应解析器（NBMCParser、JPHParser、NSHParser）；
4. 使用传统解析器提取患者信息和检验指标；
5. 标准化处理后统一入库；
6. 记录降级原因，返回前端展示。

***

# 第 7 章 MySQL 数据库完整设计

## 7.1 数据库基础配置

数据库名：LabReport；开发账号 root，密码 root；字符集 utf8mb4；排序规则 utf8mb4\_unicode\_ci；引擎 InnoDB；开启事务、外键约束。

## 7.2 ER 实体关系说明

1. Hospital（医院）1:N Report（报告）；
2. Report 1:N ReportPage（报告页面）；
3. ReportPage 1:N OCRBlock（OCR 文本块）；
4. Report 1:N LabResult（检验结果）；
5. LabItem（标准指标）1:N Alias（别名）；
6. LabItem 1:N LabResult；
7. UploadTask（上传任务）1:N Report；
8. User（用户）N:M Role（角色）；
9. 所有修改操作对应一条 AuditLog 审计日志。

## 7.3 核心数据表结构

### DB-01 Hospital 医院信息表

hospital\_id (主键自增)、hospital\_name、province、city、parser\_code、create\_time、update\_time

### DB-02 UploadTask 上传任务表

task\_id、user\_id、file\_total、success\_count、fail\_count、status、progress、message、start\_time、finish\_time

### DB-03 Report 报告主表

report\_id (全局唯一主键)、task\_id、hospital\_id、patient\_name、gender、age、sample\_time、report\_time、file\_name、file\_type、file\_size、file\_md5、storage\_path、thumbnail\_path、page\_count、quality\_score、review\_status、is\_delete、create\_time

### DB-04 ReportPage 报告页表

page\_id、report\_id、page\_no、preview\_image\_path、width、height、create\_time

### DB-05 OCRBlock OCR 文本块表

block\_id、page\_id、text、confidence、bbox\_left、bbox\_top、bbox\_right、bbox\_bottom

### DB-06 LabItem 标准检验项目表

item\_id、item\_name、abbr、english\_name、category、standard\_unit、description

### DB-07 Alias 指标别名映射表

alias\_id、item\_id、alias\_name

### DB-08 LabResult 检验结果核心表

result\_id、report\_id、page\_id、item\_id、raw\_item\_name、raw\_value、value\_numeric、unit、reference\_low、reference\_high、reference\_text、flag、bbox\_left、bbox\_top、bbox\_right、bbox\_bottom、ocr\_confidence、review\_status、source\_file、source\_path

### DB-09 User 用户表

user\_id、username、password (加密)、real\_name、is\_enable、create\_time

### DB-10 Role 角色表

role\_id、role\_name、permission\_list

### DB-11 UserRole 用户角色关联表

user\_id、role\_id

### DB-12 AuditLog 审计日志表

log\_id、operate\_user\_id、operate\_type、target\_table、target\_id、old\_value、new\_value、operate\_time、ip\_address

## 7.4 索引设计

1. Report：report\_id (主键)、file\_md5、hospital\_id、sample\_time；
2. LabResult：report\_id、item\_id、page\_id；
3. OCRBlock：page\_id；
4. AuditLog：target\_table、target\_id、operate\_time；
5. UploadTask：task\_id、user\_id。

## 7.5 数据分区规划

针对百万级检验数据，采用年度分区策略提升查询性能：

1. **Report 表**：按 sample\_time 年份分区，如 `report_2025`、`report_2026`；
2. **LabResult 表**：跟随 Report 表关联分区；
3. **历史数据归档**：5 年以上数据迁移至归档库 `lab_report_archive`，保持在线库精简；
4. **归档数据查询**：提供归档查询接口，跨年度查询自动路由到对应分区。

## 7.6 备份策略

1. **每日全量备份**：凌晨 3:00 自动执行 `mysqldump`，压缩存储至 `backup/mysql/` 目录；
2. **实时 binlog**：MySQL binlog 启用 Row 模式，支持任意时间点数据恢复；
3. **保留周期**：全量备份保留 30 天，binlog 保留 90 天；
4. **定期恢复演练**：每月执行一次备份恢复测试，验证备份文件可用性；
5. **文件备份**：上传原始文件每周增量备份，保留 30 天。

***

# 第 8 章 文件存储体系设计

## 8.1 存储根目录

`storage/`，下分 5 个子目录隔离资源：

1. `storage/report/`：原始上传 PDF / 图片，按年份 / 月份 /report\_id 分层存储；
2. `storage/preview/`：页面高清预览图，路径 `{report_id}/page_{page_no}.png`；
3. `storage/thumbnail/`：报告首页缩略图，`{report_id}.jpg`；
4. `storage/ocr/`：OCR 识别 JSON 缓存文件；
5. `storage/export/`：临时导出 Excel/CSV 文件，定时自动清理。

## 8.2 文件命名规范

所有资源绑定全局唯一 report\_id，避免文件名冲突；原始文件保留原名，增加前缀 `report_id_`。

## 8.3 缓存清理策略

配置过期天数，定时任务删除超过有效期的预览、OCR、导出临时文件；原始报告文件永久保留，仅支持手动软删除数据库记录。

## 8.4 StorageManager 统一接口

StorageManager 为文件操作的统一门面，隔离业务层与底层存储实现：

1. **统一文件读写接口**：所有业务模块通过 StorageManager 进行文件读写，禁止直接操作磁盘路径；
2. **缩略图生成**：封装 PDF 首页缩略图、页面高清预览图生成逻辑；
3. **文件归档**：超过保留期限的原始文件自动归档至 `storage/archive/`；
4. **存储切换**：后续切换 MinIO / 阿里云 OSS 对象存储时，仅替换 StorageManager 内部适配器，业务代码无需修改；
5. **接口方法清单**：
   - `save_original_file(report_id, file)` → 保存原始文件
   - `get_original_path(report_id)` → 获取原始文件路径
   - `generate_thumbnail(report_id)` → 生成缩略图
   - `generate_preview(report_id, page_no)` → 生成页面预览
   - `delete_temp_files(days)` → 清理过期临时文件

***

# 第 9 章 REST API 与 WebSocket 接口设计

## 9.1 API 通用规范

1. 请求前缀 `/api/v1`；统一返回格式 `{code,msg,data}`；Token 鉴权放在请求头 `Authorization`；
2. 分页统一参数 `page`、`page_size`；时间格式统一 `yyyy-MM-dd HH:mm:ss`；

## 9.2 核心 API 分类

### API-01 用户权限接口

登录、用户列表、角色管理、权限校验、修改密码

### API-02 文件上传接口

单文件上传、文件夹批量上传、分片上传、获取上传任务进度

### API-03 解析任务接口

任务列表、重试失败任务、终止任务

### API-04 报告管理接口

报告分页查询、报告详情、软删除报告

### API-05 指标查询接口

指标明细列表、全文检索、单指标时序数据（趋势图表）

### API-06 预览接口

获取页面预览图片、获取指标 bbox 坐标、生成预览超链接

### API-07 人工复核接口

编辑指标数据、查询审计日志、指标数据回滚

### API-08 导出接口

Excel/CSV 批量导出文件流

### API-09 模板与配置接口

医院模板管理、标准指标库维护、系统参数配置

### API-10 AI 识别接口

| 接口名称 | 功能描述 | 方法 | 路径 |
| ---- | ---- | ---- | ---- |
| 获取 AI 配置 | 获取当前 AI 服务配置（API Key 脱敏） | GET | /api/v1/ai/config |
| 保存 AI 配置 | 保存 API Key、base_url、模型名、提示词 | POST | /api/v1/ai/config |
| 测试 AI 连接 | 测试大模型 API 连通性 | POST | /api/v1/ai/test |
| 单文件 AI 识别 | 上传单个文件进行 AI 识别 | POST | /api/v1/ai/recognize |
| 多文件 AI 识别 | 批量上传文件进行 AI 识别 | POST | /api/v1/ai/recognize/batch |

## 9.3 WebSocket 接口

地址 `ws://域名/ws/task`，推送实时上传进度、解析进度、任务完成通知。

***

# 第 10 章 前端页面详细设计

## 10.1 登录页面

账号密码登录，密码加密传输，记住登录状态，错误提示。

## 10.2 首页 Dashboard

统计卡片：报告总数、待复核报告、本月上传数量、异常指标总数；快捷入口：上传中心、报告管理、趋势分析、系统配置。

## 10.3 上传中心页面

拖拽上传区域、文件夹上传按钮、多文件选择上传；上传任务列表，实时进度条，失败文件重传按钮。

## 10.4 解析任务管理页面

所有异步任务列表，展示状态、进度、成功 / 失败数量、操作（重试、终止）。

## 10.5 报告管理列表页面

多条件筛选（医院、时间、文件类型、是否复核）、全文检索、批量导出、批量标记复核状态；列表展示报告基础信息、缩略图、质量分。

## 10.6 报告详情双栏页面（核心页面）

左侧：全部检验指标表格，支持筛选、搜索、编辑（增删改和保存）；右侧：PDF / 图片预览画布；交互：点击左侧任意指标，右侧画布自动跳转页面，红色框高亮指标 bbox 区域；支持预览缩放、翻页、全屏；右侧有“重新识别”按钮，点击该按钮重新识别化验单项目，并删除左侧表格内容，将重新识别的内容填进去。

### 10.6.1 重新识别功能

当医院未被识别（hospital_id 为空）时，右侧预览区域显示解析引擎下拉框，用户可手动选择解析引擎：

| 解析引擎 | 代码 | 适用医院 |
| ---- | ---- | ---- |
| 通用解析器 | common | 未识别医院 |
| 南京明基医院 | nbmc | 南京明基医院 |
| 江苏省人民医院 | jph | 江苏省人民医院 |
| 南京市第二医院 | nsh | 南京市第二医院 |

点击“重新识别”按钮时：
- 如果选择了解析引擎：使用选定的解析引擎重新解析报告；
- 如果未选择：使用自动医院识别逻辑重新解析；
- 重新解析完成后，刷新报告数据，更新指标表格。

## 10.7 趋势分析页面

选择单一标准指标，筛选时间 / 医院，生成时序折线图；展示历次数值、自动标注升降趋势，导出图表图片。

## 10.8 医院模板管理页面

模板列表、新增模板（解析代码默认值为 "common"）、编辑解析规则、启用 / 禁用模板；人工标记报告特征后系统自动学习，学习特征包括：医院名称关键字、报告页眉页脚特征、表格列名特征、参考区间格式特征、指标命名规律等。

## 10.9 标准指标库管理页面

增删改标准项目、别名、单位分类。

## 10.10 用户与权限管理页面

用户列表、新增用户、分配角色、重置密码。

## 10.11 系统配置页面

可视化修改 OCR 线程、缓存周期、备份时间、文件存储路径、数据质量阈值。

## 10.12 AI 识别页面

双标签页布局：

### 10.12.1 识别标签页
- 文件上传区域：支持单文件、多文件、目录上传；
- 实时识别进度展示：显示当前处理文件名、进度百分比、状态；
- 识别结果汇总表格：文件名、患者姓名、指标数、页数、AI 使用状态、备注、状态、查看详情按钮；
- AI 使用状态标识：AI 识别成功显示绿色"是"，降级显示橙色"否（传统解析）"；
- 降级原因展示：当使用传统解析器时，显示降级原因（如"模型未开通"、"poppler 未安装"）。

### 10.12.2 AI 配置标签页
- API Key 输入框：密码类型，支持显示/隐藏，脱敏展示；
- base_url 输入框：默认值 `https://ark.cn-beijing.volces.com/api/v3`；
- 模型名称选择/输入框；
- 提示词编辑器：多行文本框，提供默认提示词，支持自定义修改；
- 测试连接按钮：验证配置是否正确；
- 保存配置按钮：将配置持久化到数据库。

***

# 第 11 章 后端服务核心设计

## 11.1 项目代码目录结构

plaintext

```
backend/
├── api/          所有路由接口定义
├── core/         全局配置、鉴权、异常处理
├── celery_task/  异步任务定义（OCR、解析、导出、定时运维）
├── db/           数据库ORM模型、会话管理
├── parser/       PDF、图片、医院解析插件
├── ocr_engine/   PaddleOCR图像处理封装
├── standard/     指标标准化、AI纠错模块
├── ai/           AI识别服务（大模型调用、PDF转图片、降级机制）
├── storage/      文件存储、缓存管理工具
├── service/      各业务逻辑服务层
├── utils/        通用工具（MD5、文件处理、日期、加密）
├── config.yaml   系统核心配置
```

## 11.2 异步任务执行流程

前端提交上传 → API 创建 UploadTask 入库 → 提交任务 ID 至 Celery 队列 → Worker 消费任务 → 执行解析 / OCR / 标准化 → 批量写入 MySQL → 更新任务状态 → WebSocket 推送完成通知。

## 11.3 统一异常处理

全局捕获所有接口、任务异常，记录完整堆栈日志，返回标准化错误码；解析失败单独标记任务状态，记录错误原因便于重跑。

## 11.4 鉴权拦截逻辑

所有业务接口前置 Token 校验，读取用户角色权限，拦截越权访问；预览文件增加资源归属校验，禁止查看他人报告。

***

# 第 12 章 在线预览与指标定位功能设计

## 12.1 核心交互逻辑

1. 前端加载报告所有页面预览图、全量指标 bbox 坐标缓存；
2. 用户点击左侧指标行，前端读取该指标 page\_no、四坐标；
3. 预览画布切换至对应页面，根据 bbox 绘制半透明红色高亮矩形框；
4. PDF 文件通过 PDF.js 加载原始文档，直接在 PDF 图层渲染高亮框。

## 12.2 预览资源加载优化

页面缩略图预加载，高清预览图懒加载；bbox 坐标一次性从后端缓存读取，无需重复请求数据库。

## 12.3 导出预览链接实现

Excel 增加 “预览跳转链接” 字段，链接携带 report\_id、page\_no、指标 ID，打开页面自动定位高亮对应指标。

***

# 第 13 章 人工复核、审计日志设计

## 13.1 人工编辑约束

仅允许修改 LabResult 表内指标字段；修改后 review\_status 自动改为 “人工修改”；不可直接删除原始 OCR 识别内容。

## 13.2 审计日志写入规则

任意编辑操作自动插入 AuditLog 记录：操作人 ID、操作时间、操作 IP、表名、数据主键、修改前原始 JSON、修改后 JSON；日志仅支持查询，无删除、编辑接口。

## 13.3 数据回滚功能

读取审计日志 old\_value，覆盖当前指标数据，新增一条回滚操作审计记录。

## 13.4 待审核报告页面状态保持设计

### 13.4.1 页面状态信息展示

待审核报告列表页面筛选栏右侧实时展示当前页面状态信息，包括：

| 状态项       | 展示格式                             | 说明                       |
| ----------- | ---------------------------------- | -------------------------- |
| 当前页码       | `当前：第 X / Y 页`                    | X 为当前页码，Y 为总页数     |
| 筛选条件       | `筛选条件：关键词: xxx，状态: 未复核`          | 展示当前激活的所有筛选条件      |
| 总记录数       | `共 X 条记录`                          | 符合筛选条件的记录总数        |

### 13.4.2 页面状态持久化机制

采用 `localStorage` 实现页面状态保持，确保用户从审核页面返回后恢复离开时的页面状态：

1. **状态存储**：用户操作（切换页码、修改筛选条件、调整每页大小）时自动将以下数据序列化至 `localStorage`：
   - `keyword`：搜索关键词
   - `page`：当前页码
   - `pageSize`：每页记录数
   - `filterReviewStatus`：复核状态筛选值

2. **状态恢复**：页面 `onMounted` 阶段从 `localStorage` 读取并恢复上述字段，随后以恢复的参数加载数据列表

3. **返回刷新**：页面使用 `<keep-alive>` 缓存组件，`onActivated` 钩子触发数据重新加载，确保审核状态变更后列表同步更新

4. **重置清除**：点击"重置"按钮时清空筛选条件并重置页码为 1，同时更新 `localStorage` 状态

### 13.4.3 审核页面跳转与返回流程

```
用户在列表页点击"审核" → 路由跳转至 /review/{report_id} → 进入审核页面进行操作
→ 用户点击返回 → 路由回退至列表页 → onActivated 触发 → 从 localStorage 恢复页码和筛选条件
→ 以恢复的参数请求后端接口 → 页面展示与离开时完全一致的数据列表
```

## 13.5 待审核报告表格样式设计

### 13.5.1 表头样式

| 样式属性     | 值          | 说明                   |
| ----------- | ----------- | ---------------------- |
| 背景颜色     | `#1a73e8`   | 蓝色背景               |
| 文字颜色     | `#ffffff`   | 白色文字               |
| 字重         | `bold`      | 加粗                   |

### 13.5.2 行样式

| 行类型       | 背景颜色       | 说明               |
| ----------- | -------------- | ------------------ |
| 偶数行       | `#ffffff`     | 白色背景            |
| 奇数行       | `#f8f9fa`     | 浅灰色背景          |
| 悬停行       | `#e3f2fd`     | 浅蓝色高亮背景       |

### 13.5.3 实现方式

通过 Element Plus 表格的 `row-class-name` 属性为每行添加 CSS 类名（`row-even` / `row-odd`），配合 `:deep()` 穿透选择器覆盖默认样式。

## 13.6 审核统计看板

审核模块提供实时统计看板，辅助管理员掌握审核进度与质量：

1. **待审核总览**：展示当前待审核报告总数、待审核指标总数；
2. **人均工作量**：按审核人统计已完成审核数量、平均审核耗时；
3. **OCR 错误分类统计**：按错误类型（形近字、数字缺失、单位错误、参考区间异常）分类统计；
4. **审核效率趋势**：按日/周/月统计审核完成量趋势；
5. **看板接口**：`GET /api/v1/review/stats` 返回上述统计数据。

***

# 第 14 章 趋势分析与数据导出模块设计

## 14.1 趋势数据聚合逻辑

根据 item\_id 查询全部关联 LabResult，按 sample\_time 升序排序，提取时间、数值、医院，组装 ECharts 折线图数据集；自动对比相邻数值，标记上升↑、下降↓、平稳→。查询时不区分报告 `is_delete` 状态，确保已软删除报告中的检验数据仍纳入趋势分析。按 `sample_time` 日期去重，同一日期仅保留首条记录，避免重复上传产生重复数据点。

## 14.2 导出字段全集

医院名称、报告采样时间、患者姓名、标准指标缩写、标准名称、检测结果、单位、参考区间、异常标记、源文件名称、文件完整路径、页码、OCR 置信度、复核状态、网页预览超链接。

## 14.3 导出性能优化

大数据量采用流式写入 Excel，避免一次性加载全量数据占用内存；临时导出文件定时清理。

## 14.4 检验项目搜索与多指标选择设计

### 14.4.1 检验项目搜索框

采用 Element Plus 远程搜索选择器（`el-select` + `remote`），实现输入即匹配：

| 功能         | 实现方式                                                        |
| ----------- | ----------------------------------------------------------- |
| 输入匹配       | 用户输入关键词后，调用 `GET /api/v1/trend/items?keyword=xxx` 接口，后端按项目名称或缩写模糊匹配 |
| 防抖优化       | 前端 `remote-method` 添加 300ms 防抖，避免频繁请求后端                  |
| 结果展示       | 下拉列表展示匹配的指标名称、缩写、单位，每项标注 `item_id`                      |
| 请求参数格式     | 前端 `paramsSerializer` 自定义序列化，数组参数以 `item_ids=1&item_ids=7` 格式传递，兼容 FastAPI `List[int]` 参数解析 |

### 14.4.2 多指标选择

| 功能           | 实现方式                                                        |
| ------------- | ----------------------------------------------------------- |
| 多选模式         | `el-select` 设置 `multiple` 属性，允许用户同时选择多个检验项目          |
| 多指标查询接口     | `GET /api/v1/trend/analysis?item_ids=1&item_ids=7&item_ids=37`，后端遍历 `item_ids` 列表分别查询并聚合返回 |
| 颜色区分         | ECharts 自动为每个指标分配不同颜色 series，图例可切换显示/隐藏单个指标          |
| 数据隔离         | 每个指标独立计算趋势（上升/下降/平稳），互不干扰                            |

### 14.4.3 后端接口定义

**获取趋势分析项目列表**

```
GET /api/v1/trend/items?keyword={keyword}
```

| 参数       | 类型     | 必填 | 说明                  |
| --------- | -------- | ---- | --------------------- |
| keyword   | string   | 否   | 搜索关键词，匹配项目名称或缩写 |

**获取多指标趋势数据**

```
GET /api/v1/trend/analysis?item_ids={id1}&item_ids={id2}
```

| 参数       | 类型         | 必填 | 说明                          |
| --------- | ------------ | ---- | ----------------------------- |
| item_ids  | List[int]    | 是   | 检验项目 ID 列表，支持多个           |

返回数据结构中每个数据点包含 `report_id` 字段，用于前端跳转报告审核页面。

## 14.5 趋势图表交互设计

### 14.5.1 网格线配置

| 配置项           | 值                           | 说明                   |
| --------------- | ---------------------------- | ---------------------- |
| X 轴网格线        | `splitLine.show: true`      | 显示纵向网格线             |
| X 轴网格线样式     | `type: 'dashed'`            | 虚线样式                 |
| Y 轴网格线        | `splitLine.show: true`      | 显示横向网格线             |
| Y 轴网格线样式     | `type: 'dashed'`            | 虚线样式                 |

### 14.5.2 数据点点击跳转报告审核

用户点击图表上的数据点时，前端获取该数据点对应的 `report_id`，弹出确认对话框，用户确认后通过 Vue Router 跳转至报告审核页面：

```
点击数据点 → 获取 report_id → 弹出确认对话框（ElMessageBox.confirm）
→ 用户确认 → router.push(`/review/${report_id}`) → 进入报告审核页面
```

该功能实现了从趋势分析到报告审核的快速定位，用户可直接在趋势图中发现异常数据点并跳转审核。

### 14.5.3 图表缩放与区域选择

采用 ECharts 内置 `dataZoom` 和工具栏 `toolbox` 实现多维度缩放交互：

| 缩放方式           | 实现方式                                                        | 说明                              |
| ----------------- | ----------------------------------------------------------- | --------------------------------- |
| 底部滑块缩放       | `dataZoom` 组件 `type: 'slider'`                              | 拖动滑块调整 X 轴时间范围              |
| 鼠标滚轮缩放       | `dataZoom` 组件 `type: 'inside'`                              | 在图表区域内滚轮缩放，支持平移          |
| 区域框选缩放       | `toolbox.feature.dataZoom`                                  | 右上角工具栏点击"区域缩放"后框选区域放大 |
| 重置缩放           | `toolbox.feature.restore`                                   | 点击"还原"按钮恢复初始视图            |

放大时图表自动显示更多数据细节（如数据点标签、tooltip 详情），缩小后隐藏细节避免遮挡。

### 14.5.4 Tooltip 增强

鼠标悬停数据点时展示详细信息：

| 信息字段     | 说明                           |
| ----------- | ------------------------------ |
| 指标名称     | 该数据点所属检验项目名称        |
| 数值         | `value_numeric` 转换后的数值    |
| 单位         | 检验项目标准单位                |
| 采样时间     | `sample_time` 格式化显示        |
| 医院         | 检验医院名称                    |
| 趋势方向     | 上升↑ / 下降↓ / 平稳→          |
| 异常标记     | flag 字段（如 ↑↓ H L 等）       |

## 14.6 趋势分析数据源说明

### 14.6.1 数据来源

趋势分析数据全部来源于 `LabResult` 表的化验单解析结果，核心字段说明：

| 字段             | 类型              | 说明                                         |
| --------------- | ----------------- | -------------------------------------------- |
| `raw_value`     | String(100)       | OCR 识别的原始值字符串，保留 `>`、`<`、`H`、`L` 等标记 |
| `value_numeric` | DECIMAL(18,4)     | 从 `raw_value` 解析出的纯数值，用于趋势计算和绘图     |
| `item_id`       | Integer           | 关联 `LabItem` 标准指标库 ID                    |
| `report_id`     | String(64)        | 关联 `Report` 表，用于跳转报告审核页面              |
| `flag`          | String(10)        | 异常标记（↑ ↓ H L 等）                          |

### 14.6.2 数值转换规则

`value_numeric` 由 `raw_value` 通过 `parse_numeric()` 函数转换得到：

| raw_value   | value_numeric | 转换规则                  |
| ----------- | -------------- | ------------------------- |
| `42.50`     | `42.5`         | 纯数字直接转换             |
| `>1000`     | `1000`         | 去除 `>` 符号              |
| `<0.5`      | `0.5`          | 去除 `<` 符号              |
| `12.34H`    | `12.34`        | 去除 `H`（高）标记          |
| `5.2L`      | `5.2`          | 去除 `L`（低）标记          |

## 14.7 数据库连接锁优化

### 14.7.1 问题背景

SQLite 数据库在并发写入时存在锁竞争问题。后端启动时后台线程解析未解析报告，持续占用数据库写锁，导致用户在报告审核页面保存数据时出现 `sqlite3.OperationalError: database is locked` 错误。

### 14.7.2 优化方案

| 优化项                     | 实现方式                                                        | 说明                                |
| ------------------------- | ----------------------------------------------------------- | ----------------------------------- |
| 连接超时参数               | `connect_args={"timeout": 30}`                              | 连接等待锁释放最长 30 秒              |
| 解析间隔延迟               | `time.sleep(1)` 每个报告解析完成后暂停 1 秒                  | 释放数据库锁，让其他请求有机会获取      |
| 解析内休眠                 | `time.sleep(0.5)` 单个报告解析完成后暂停 0.5 秒              | 降低锁持有频率                        |

***

# 第 15 章 系统配置、模板与权限管理设计

## 15.1 系统配置持久化

核心参数存储数据库系统配置表，后端启动加载至内存缓存，前端可视化修改后实时更新缓存，无需重启服务。

## 15.2 医院模板插件化

模板配置 yaml 存储模板特征、字段提取规则；新增医院仅添加配置文件与轻量解析子类，不改动核心解析流程。解析器支持两种实现方式：
- OCR 坐标解析：适用于扫描版 PDF，通过 OCR 识别的文本块坐标（bbox）来定位和提取表格数据，支持单列、双列等多种布局；
- 纯文本解析：适用于文字可提取 PDF，直接通过文本行匹配和正则表达式提取数据。
人工标记报告特征后系统自动学习，学习特征包括：医院名称关键字、报告页眉页脚特征、表格列名特征、参考区间格式特征、指标命名规律等。新增医院时，parser_code 字段默认值为 "common"，使用通用解析器。

## 15.3 RBAC 权限模型

角色预设权限编码列表；接口校验当前用户角色是否包含对应权限编码；超级管理员拥有全部权限，不可限制。

***

# 第 16 章 部署架构（Docker Compose）

## 16.1 容器清单

1. nginx：反向代理、静态资源托管、HTTPS；
2. frontend：Vue 前端打包产物；
3. backend：FastAPI 主服务；
4. celery-worker：异步任务消费进程；
5. redis：缓存与任务队列；
6. mysql8：业务数据库。

## 16.2 部署流程

安装 Docker、Docker Compose → 配置 docker-compose.yml、config.yaml、Nginx 配置 → 一键执行 `docker-compose up -d` → 初始化 MySQL 库表、导入标准指标字典 → 访问前端页面登录使用。

## 16.3 数据库配置区分

开发环境硬编码 root/root；生产环境通过 docker 环境变量注入账号密码，配置文件禁止明文存储数据库凭证。

***

# 第 17 章 测试方案设计

## 17.1 单元测试

覆盖工具类、解析函数、标准化匹配逻辑、OCR 预处理函数；使用 pytest 自动化执行。

## 17.2 集成测试

接口全链路测试：上传 - 解析 - 入库 - 预览 - 导出完整流程，校验数据一致性。

## 17.3 OCR 专项测试

多种清晰度拍照图片、黑白 / 彩色扫描件、多医院报告，统计识别准确率、数据质量分达标率。

## 17.4 性能测试

并发上传批量文件，测试解析吞吐量、接口响应时间、内存占用。

## 17.5 验收测试

按照需求文档逐条验证功能，核对全部业务场景、溯源、预览、权限流程。

***

# 第 18 章 运维设计

## 18.1 自动备份

定时每日凌晨执行 MySQL 全量备份，压缩存储至 backup 目录；保留 30 天备份文件；原始文件目录增量备份。

## 18.2 日志运维

系统日志按日期分文件存储，自动压缩归档；提供日志查询接口，支持按时间、错误等级检索。

## 18.3 故障恢复

数据库故障：导入备份 sql 恢复；文件丢失：重新上传文件触发重新解析生成缓存；服务崩溃：Docker 容器自动重启。

## 18.4 磁盘监控定时任务

定时检测 storage 目录磁盘占用，超过阈值输出告警日志。

## 18.5 分级告警机制

系统建立三级告警响应机制，确保问题及时发现和处理：

| 告警级别 | 响应时间 | 通知渠道 | 典型场景 |
|---------|---------|---------|---------|
| P0 致命 | 5 分钟内 | 电话 + 短信 + 钉钉 | 服务不可用、数据库宕机、磁盘满 |
| P1 严重 | 30 分钟内 | 钉钉 + 企业微信 | OCR 成功率 < 80%、队列堆积 > 100 |
| P2 普通 | 工作时段 | 企业微信 + 邮件 | 磁盘占用 > 80%、解析耗时超标 |

## 18.6 健康检查接口

提供系统健康检查端点，供容器存活探测和监控系统调用：

- **接口**：`GET /api/v1/system/health`
- **返回内容**：
  ```json
  {
    "status": "ok",
    "timestamp": "2026-07-23T10:00:00",
    "services": {
      "database": "ok",
      "redis": "ok",
      "ocr_engine": "ok",
      "storage": "ok"
    },
    "stats": {
      "pending_reports": 13,
      "today_parsed": 87,
      "disk_usage": 62.5
    }
  }
  ```

## 18.7 发布回滚机制

1. **镜像版本化**：每次发布生成唯一版本号（如 `v3.0.6-20260723`），Docker 镜像带版本标签；
2. **数据库版本管理**：`sql/` 目录按版本号存放升级脚本，配套回滚 SQL；
3. **一键回滚**：若新版本出现严重问题，执行 `docker compose up -d --no-deps backend` 切换至上一版本镜像；
4. **灰度发布**：支持按用户比例逐步发布新版本，发现问题立即回滚。

***

# 第 19 章 产品迭代 Roadmap

## V3.0（当前版本）

完成前后端分离、PDF / 图片解析、目录批量上传、指标预览定位、MySQL 存储、人工复核、趋势导出、Docker 部署、RBAC 权限。

## V3.5 迭代规划

1. AI 深度自学习医院模板，自动生成解析规则；
2. 批量健康汇总报告生成（HTML/PDF）；
3. 高级筛选统计、异常指标汇总报表；
4. 优化 OCR 模糊文字识别准确率。

## V4.0 长期规划

1. 多租户数据隔离；
2. FHIR/HL7 医疗标准对外接口；
3. 小程序 / APP 配套客户端；
4. 内置 AI 健康解读模块（免责声明强化）；
5. 分布式集群部署，支持海量报告存储解析。

### V4.0 AI 扩展详细规划

#### 视觉 LLM 直接解析

接入视觉大模型（如 GPT-4V、火山方舟视觉模型），实现：
- 直接解析不规则非标化验单图片/PDF，无需医院模板；
- 自动识别新医院版式，生成解析模板配置文件；
- 修正 OCR 无法处理的复杂排版、手写体识别。

#### RAG 医学知识库

构建检验指标知识库，提供：
- 指标临床参考范围说明（如 ALT 升高常见于哪些疾病）；
- 指标联合解读（如 ALT + AST 同时升高提示肝细胞损伤）；
- 基于权威医学文献的证据链引用。

#### Agent 自动化

实现智能体自动化工作流：
- Agent 自动完成批量解析任务调度、失败重试、结果汇总；
- Agent 根据用户自然语言查询指标历史数据（如"最近三个月血糖变化"）；
- Agent 自动生成健康汇总报告并导出；
- Prompt 管理：管理员可配置不同场景的 Agent 提示词模板。

#### AI 安全约束

1. AI 输出仅为辅助建议，高危异常必须人工复核；
2. 患者敏感数据自动脱敏后传入模型；
3. 所有 AI 决策留痕，记录输入、输出、模型版本。

***

# 附录 A MySQL 完整建表 SQL

sql

```
CREATE DATABASE IF NOT EXISTS LabReport DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE LabReport;

-- 医院表
CREATE TABLE Hospital (
    hospital_id INT AUTO_INCREMENT PRIMARY KEY,
    hospital_name VARCHAR(200) NOT NULL,
    province VARCHAR(50),
    city VARCHAR(50),
    parser_code VARCHAR(100) NOT NULL,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB;

-- 上传任务表
CREATE TABLE UploadTask (
    task_id VARCHAR(64) PRIMARY KEY,
    user_id INT NOT NULL,
    file_total INT DEFAULT 0,
    success_count INT DEFAULT 0,
    fail_count INT DEFAULT 0,
    status TINYINT COMMENT '0待处理 1处理中 2完成 3失败',
    progress FLOAT DEFAULT 0,
    message TEXT,
    start_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    finish_time DATETIME NULL
) ENGINE=InnoDB;

-- 报告主表
CREATE TABLE Report (
    report_id VARCHAR(64) PRIMARY KEY,
    task_id VARCHAR(64) NOT NULL,
    hospital_id INT NULL,
    patient_name VARCHAR(50),
    gender VARCHAR(10),
    age VARCHAR(20),
    sample_time DATETIME,
    report_time DATETIME,
    file_name VARCHAR(255) NOT NULL,
    file_type VARCHAR(20) NOT NULL,
    file_size BIGINT,
    file_md5 VARCHAR(64) NOT NULL,
    storage_path VARCHAR(500) NOT NULL,
    thumbnail_path VARCHAR(500),
    page_count INT DEFAULT 1,
    quality_score FLOAT,
    review_status TINYINT COMMENT '0未复核 1已复核 2人工修改',
    is_delete TINYINT DEFAULT 0,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_task(task_id),
    INDEX idx_hospital(hospital_id),
    INDEX idx_md5(file_md5),
    INDEX idx_sample_time(sample_time)
) ENGINE=InnoDB;

-- 报告页表
CREATE TABLE ReportPage (
    page_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    report_id VARCHAR(64) NOT NULL,
    page_no INT NOT NULL,
    preview_image_path VARCHAR(500),
    width INT,
    height INT,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_report(report_id)
) ENGINE=InnoDB;

-- OCR文本块
CREATE TABLE OCRBlock (
    block_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    page_id BIGINT NOT NULL,
    text TEXT NOT NULL,
    confidence FLOAT,
    bbox_left INT,
    bbox_top INT,
    bbox_right INT,
    bbox_bottom INT,
    INDEX idx_page(page_id)
) ENGINE=InnoDB;

-- 标准检验项目
CREATE TABLE LabItem (
    item_id INT AUTO_INCREMENT PRIMARY KEY,
    item_name VARCHAR(100) NOT NULL,
    abbr VARCHAR(50),
    english_name VARCHAR(200),
    category VARCHAR(50),
    standard_unit VARCHAR(30),
    description TEXT
) ENGINE=InnoDB;

-- 指标别名
CREATE TABLE Alias (
    alias_id INT AUTO_INCREMENT PRIMARY KEY,
    item_id INT NOT NULL,
    alias_name VARCHAR(100) NOT NULL,
    INDEX idx_item(item_id)
) ENGINE=InnoDB;

-- 检验结果核心表
CREATE TABLE LabResult (
    result_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    report_id VARCHAR(64) NOT NULL,
    page_id BIGINT NOT NULL,
    item_id INT NULL,
    raw_item_name VARCHAR(100) NOT NULL,
    raw_value VARCHAR(100),
    value_numeric DECIMAL(18,4) NULL,
    unit VARCHAR(30),
    reference_low DECIMAL(18,4) NULL,
    reference_high DECIMAL(18,4) NULL,
    reference_text VARCHAR(200),
    flag VARCHAR(10) COMMENT '↑ ↓ 正常',
    bbox_left INT,
    bbox_top INT,
    bbox_right INT,
    bbox_bottom INT,
    ocr_confidence FLOAT,
    review_status TINYINT DEFAULT 0,
    source_file VARCHAR(255) NOT NULL,
    source_path VARCHAR(500) NOT NULL,
    INDEX idx_report(report_id),
    INDEX idx_item(item_id),
    INDEX idx_page(page_id)
) ENGINE=InnoDB;

-- 用户表
CREATE TABLE User (
    user_id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50) UNIQUE NOT NULL,
    password VARCHAR(200) NOT NULL,
    real_name VARCHAR(50),
    is_enable TINYINT DEFAULT 1,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB;

-- 角色表
CREATE TABLE Role (
    role_id INT AUTO_INCREMENT PRIMARY KEY,
    role_name VARCHAR(50) UNIQUE NOT NULL,
    permission_list TEXT
) ENGINE=InnoDB;

-- 用户角色关联
CREATE TABLE UserRole (
    user_id INT NOT NULL,
    role_id INT NOT NULL,
    PRIMARY KEY(user_id, role_id)
) ENGINE=InnoDB;

-- 审计日志表
CREATE TABLE AuditLog (
    log_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    operate_user_id INT,
    operate_type VARCHAR(30) NOT NULL,
    target_table VARCHAR(50) NOT NULL,
    target_id VARCHAR(64) NOT NULL,
    old_value TEXT,
    new_value TEXT,
    operate_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    ip_address VARCHAR(50),
    INDEX idx_target(target_table, target_id),
    INDEX idx_time(operate_time)
) ENGINE=InnoDB;
```

# 附录 B 系统核心配置 config.yaml 模板

yaml

```
# 数据库配置
database:
  host: localhost
  port: 3306
  db_name: LabReport
  user: root
  password: root # 生产环境使用环境变量注入，不写死

# Redis配置
redis:
  host: redis
  port: 6379
  db: 0

# 文件存储根路径
storage_root: "./storage"
cache_expire_days: 30
auto_backup_time: "01:00"
quality_score_threshold: 85

# OCR配置
ocr_thread_num: 4
enable_ai_correct: true

# 服务端口
api_port: 8000
ws_port: 8000
```

# 附录 C 错误码全集定义

表格

| 错误码   | 说明             |
| ----- | -------------- |
| 0     | 请求成功           |
| 10001 | 用户未登录，Token 失效 |
| 10002 | 无接口访问权限        |
| 20001 | 文件格式不支持        |
| 20002 | 文件损坏 / 空文件     |
| 20003 | 重复文件 MD5 已存在   |
| 30001 | OCR 识别失败       |
| 30002 | 解析任务执行异常       |
| 40001 | 查询参数非法         |
| 50001 | 服务器内部异常        |

# 附录 D OpenAPI 规范说明

后端 FastAPI 框架自动生成 OpenAPI 标准接口文档，访问地址 `/docs`、`/redoc`；统一遵循 RESTful 规范，包含所有接口请求参数、请求体、响应示例、权限标识；支持前端基于 OpenAPI 自动生成接口请求代码。

# 附录 E 缩略词对照表

表格

| 缩写        | 全称                            | 释义           |
| --------- | ----------------------------- | ------------ |
| LRP       | LabReportParser               | 医学检验报告解析管理平台 |
| OCR       | Optical Character Recognition | 光学字符识别       |
| RBAC      | Role-Based Access Control     | 基于角色的权限控制    |
| ORM       | Object Relational Mapping     | 对象关系映射       |
| CSV       | Comma-Separated Values        | 逗号分隔文本导出文件   |
| MD5       | Message-Digest Algorithm 5    | 文件唯一校验哈希算法   |
| WebSocket | WebSocket Protocol            | 前后端实时长连接通信协议 |

***

# 第 20 章 系统安全设计

## 20.1 身份认证

采用 JWT 双令牌机制保障系统安全：

1. **Access Token**：有效期 2 小时，用于接口鉴权；
2. **Refresh Token**：有效期 7 天，用于刷新 Access Token；
3. **登录保护**：连续 5 次登录失败后锁定账户 30 分钟；
4. **密码加密**：使用 BCrypt 算法存储密码哈希，不可逆；
5. **设备绑定**：可选开启设备绑定，防止 Token 被盗用。

## 20.2 RBAC 权限模型

四级角色体系，菜单、按钮、数据范围分级隔离：

| 角色 | 权限范围 | 典型用户 |
|------|---------|---------|
| Admin 管理员 | 全部功能 | 系统管理员 |
| Doctor 医生 | 查看/审核/导出 | 医护人员 |
| User 普通用户 | 上传/查看/自己的数据 | 个人用户 |
| Viewer 只读 | 仅查看报告 | 只读角色 |

## 20.3 数据隐私保护

1. **敏感字段加密**：患者身份证号、手机号等敏感字段采用 AES-256 加密存储；
2. **日志脱敏**：审计日志中敏感信息自动脱敏处理；
3. **数据隔离**：用户仅能访问自己上传/负责的数据；
4. **数据导出权限**：导出操作需单独权限，审计日志全程记录。

## 20.4 Web 安全防护

1. **HTTPS 强制**：生产环境强制 HTTPS 加密传输；
2. **XSS 防护**：前端输出统一 HTML 转义；
3. **SQL 注入防护**：ORM 参数化查询，禁止拼接 SQL；
4. **CSRF 防护**：Cookie SameSite + Token 双重校验；
5. **CORS 策略**：限制允许的 Origin 来源；
6. **安全响应头**：Content-Security-Policy、X-Frame-Options 等。

## 20.5 文件上传安全

1. **文件类型校验**：白名单限制（PDF/JPG/PNG/BMP/TIFF/WEBP）；
2. **文件大小限制**：单文件最大 100MB；
3. **可执行文件拦截**：黑名单拦截 exe/bat/sh/com 等可执行文件；
4. **文件哈希校验**：上传后计算 MD5/SHA256，校验文件完整性；
5. **存储路径隔离**：上传文件存储在专用目录，禁止执行。

## 20.6 审计日志安全

1. **全量审计**：所有新增、修改、删除、配置变更操作永久记录；
2. **日志不可篡改**：审计日志仅增不改，禁止物理删除；
3. **日志内容**：操作模块、操作类型、业务 ID、操作人、IP、修改前后 JSON、操作时间。

## 20.7 数据库安全

1. **最小权限原则**：生产环境禁用 root 账号，分配最小权限业务账号；
2. **SSL 加密连接**：数据库连接启用 SSL 加密传输；
3. **慢查询日志**：开启 slow query log，监控潜在性能问题；
4. **定期审计**：定期审查数据库权限，回收不必要授权。

## 20.8 配置安全

1. **敏感配置分离**：数据库密码、API 密钥存入 `.env` 文件；
2. **.env 不入库**：`.env` 文件加入 `.gitignore`，禁止提交 Git；
3. **配置加密**：敏感配置项支持 AES 加密存储；
4. **配置变更审计**：所有配置变更记录审计日志。

***

# 第 21 章 性能优化设计

## 21.1 异步任务分离

将耗时操作与 Web 请求解耦，全部采用 Celery 异步队列处理：

1. **OCR 识别任务**：扫描 PDF 的 OCR 处理提交至异步队列；
2. **PDF 渲染任务**：高清预览图、缩略图生成异步执行；
3. **Excel 导出任务**：大数据量导出生成临时文件后返回下载链接；
4. **报告解析任务**：批量报告解析通过 WebSocket 推送进度。

## 21.2 多线程池隔离

为不同耗组件配备独立线程池，避免资源抢占：

| 线程池 | 线程数 | 用途 |
|--------|-------|------|
| OCR 线程池 | CPU 核心 × 2 | PaddleOCR 并发识别 |
| PDF 渲染线程池 | CPU 核心 | PDF 转图片、缩略图生成 |
| 解析线程池 | CPU 核心 | 报告解析与标准化 |
| Web 线程池 | 固定 8 线程 | FastAPI Web 请求处理 |

## 21.3 多级缓存策略

1. **磁盘缓存**：OCR 识别结果、PDF 渲染结果持久化至磁盘缓存；
2. **Redis 缓存**：标准指标字典、系统配置、热点查询结果缓存；
3. **内存缓存**：医院模板、解析器实例缓存，复用解析器避免重复加载；
4. **缓存失效**：配置变更时自动失效对应缓存，支持手动刷新。

## 21.4 数据库优化

1. **合理索引**：Report 表 (hospital_id, sample_time) 联合索引，LabResult 表 (report_id, item_id) 联合索引；
2. **批量写入**：解析结果采用 `bulk_insert_mapping` 批量写入，替代循环单条插入；
3. **大数据分页**：分页查询使用游标分页，避免深分页性能问题；
4. **历史数据归档**：5 年以上数据归档至历史库，保持在线库精简。

## 21.5 OCR 专项优化

1. **缓存复用**：相同文件 SHA256 + 页码命中缓存时直接读取，不重复 OCR；
2. **大图缩放**：超过 4000×4000 像素的图片自动等比缩放；
3. **文字 PDF 跳过**：经判定为文字 PDF 的页面直接提取文本，不调用 OCR；
4. **GPU 加速**：支持 GPU CUDA 加速，单页 OCR 耗时 ≤ 0.8s。

## 21.6 前端性能优化

1. **路由懒加载**：按需加载页面组件，减少初始加载体积；
2. **虚拟滚动表格**：大数据量表格采用虚拟滚动，仅渲染可见行；
3. **图片懒加载**：预览图、缩略图采用懒加载；
4. **ECharts 数据缓存**：趋势图表数据前端缓存，避免重复请求；
5. **接口缓存**：字典类、配置类接口前端缓存，设置合理过期时间。

## 21.7 量化性能指标

| 指标 | 目标值 |
|------|-------|
| 单页 OCR 识别 | ≤ 0.8s (GPU) |
| 单份报告解析 | ≤ 5s |
| 报告列表分页查询 | ≤ 300ms |
| 趋势分析查询 | ≤ 500ms |
| 10000 条数据 Excel 导出 | ≤ 10s |
| 系统年可用率 | ≥ 99.9% |

***

# 第 22 章 项目实施与版本规划

## 22.1 开发阶段划分

| 阶段 | 内容 | 交付物 |
|------|------|--------|
| M1 基础框架 | 用户、数据库、核心路由搭建 | 可运行的骨架工程 |
| M2 上传解析 | 文件上传、OCR、PDF 分流处理 | 上传中心可批量解析 |
| M3 标准化预览 | 指标标准化、在线预览、指标定位 | 完整的解析→预览链路 |
| M4 审核导出 | 人工复核、审计日志、趋势分析、导出 | 核心业务闭环 |
| M5 优化上线 | 性能优化、全量测试、Docker 部署 | 生产就绪版本 |

## 22.2 项目组织架构

| 角色 | 职责 |
|------|------|
| 项目经理 | 需求对接、进度管控、风险协调 |
| 产品经理 | 需求分析、原型设计、用户验收 |
| 后端工程师 | API 设计、解析引擎、数据库设计 |
| 前端工程师 | 页面开发、交互实现、性能优化 |
| OCR/AI 工程师 | OCR 调优、AI 模型对接、提示词工程 |
| 测试工程师 | 测试用例设计、自动化测试、性能测试 |
| 运维工程师 | 部署架构、监控告警、备份恢复 |

## 22.3 新增医院实施流程

当需要支持新医院时，遵循以下标准流程：

1. **样本收集**：收集该医院 20-50 份不同类型化验单样本；
2. **版式分析**：分析表格布局、列名格式、参考区间格式、页眉页脚特征；
3. **编写解析器**：继承 BaseParser 编写专属解析器插件 + yaml 配置；
4. **单元测试**：基于样本编写单元测试，指标识别准确率 ≥ 99%；
5. **灰度上线**：先在测试环境验证，通过后上线生产环境；
6. **上线监控**：上线后 7 天内密切监控解析质量，收集异常报告。

## 22.4 风险管控

| 风险类型 | 应对措施 |
|---------|---------|
| OCR 识别模糊 | 引入 AI 辅助纠错，低置信标记人工审核 |
| 新医院版式 | 预留通用解析器兜底，逐步积累模板 |
| 大批量性能 | 异步队列 + 多线程池 + 缓存三级优化 |
| 人员变动 | 统一代码规范、详细文档、Git 版本管理 |
| 数据丢失 | 每日全量备份 + 实时 binlog + 定期恢复演练 |

## 22.5 交付物清单

1. **前后端完整源码**：包含后端服务、前端页面、配置文件；
2. **Docker Compose 部署脚本**：一键启动全环境；
3. **MySQL 初始化/升级 SQL**：建表脚本 + 升级迁移脚本；
4. **全套设计文档**：本文档为核心，配套 API 文档、部署手册；
5. **用户操作手册**：面向普通用户、审核员、管理员；
6. **运维手册**：部署、监控、备份、恢复 SOP；
7. **全套测试报告**：单元测试、集成测试、性能测试报告。

## 22.6 长期扩展方向

- **医疗对接**：HIS/LIS/EMR 系统对接、FHIR/HL7 标准接口；
- **AI 智能化**：AI 报告解读、自动模板生成、自然语言查询；
- **SaaS 多租户**：集团化部署、数据隔离、租户级配置；
- **云原生**：K8s 集群部署、弹性伸缩、Service Mesh；
- **移动端**：小程序、APP 配套，支持拍照即解析。

***

# 附录 F 术语对照表

| 缩写 | 全称 | 释义 |
|------|------|------|
| OCR | Optical Character Recognition | 光学字符识别 |
| LLM | Large Language Model | 大语言模型 |
| RAG | Retrieval-Augmented Generation | 检索增强生成 |
| RBAC | Role-Based Access Control | 基于角色的权限控制 |
| JWT | JSON Web Token | JSON 网络令牌 |
| SDS | Software Design Specification | 软件详细设计说明书 |
| SLA | Service Level Agreement | 服务等级协议 |
| RTO | Recovery Time Objective | 灾难恢复时间目标 |
| RPO | Recovery Point Objective | 灾难恢复点目标 |
| BBox | Bounding Box | 边界框（OCR 坐标） |
| LIS | Laboratory Information System | 实验室信息系统 |
| HIS | Hospital Information System | 医院信息系统 |
| EMR | Electronic Medical Record | 电子病历 |
| FHIR | Fast Healthcare Interoperability Resources | 快速医疗互操作资源 |
| CEF | Chromium Embedded Framework | 嵌入式浏览器框架 |

# 附录 G 代码规范

## G.1 Python 后端规范

1. **PEP 8**：遵循 PEP 8 编码规范；
2. **代码格式化**：使用 Black 自动格式化，行宽 100；
3. **类型标注**：所有函数参数和返回值使用 Type Hint 类型标注；
4. **命名规范**：模块 snake_case、类 PascalCase、函数 snake_case、常量 UPPER_SNAKE_CASE；
5. **注释规范**：公共函数/类必须有 docstring，复杂逻辑添加行内注释；
6. **错误处理**：统一异常处理，禁止裸 except，必须记录日志；
7. **依赖管理**：requirements.txt 固定版本号，禁止使用 `latest`。

## G.2 Vue3 前端规范

1. **ESLint + Prettier**：统一代码风格，保存时自动格式化；
2. **组件命名**：PascalCase，多单词组成，如 `ReviewList.vue`；
3. **组合式 API**：使用 `<script setup>` 或 Composition API；
4. **状态管理**：Pinia Store 命名 useXxxStore；
5. **路由命名**：路由 name 与组件名对应，如 `review-list`；
6. **API 调用**：统一通过 `api/` 目录封装，禁止在组件内直接写 axios 请求；
7. **样式规范**：优先使用 Element Plus 主题变量，自定义样式使用 BEM 命名。

## G.3 Git 协作规范

1. **分支管理**：采用 Git Flow，main（生产）→ develop（开发）→ feature/*（功能分支）；
2. **Commit 规范**：`type(scope): description`，如 `feat(review): 添加审核统计看板`；
3. **Code Review**：所有合并请求必须经至少一人 Review；
4. **禁止提交**：`.env`、`dist/`、`__pycache__/`、IDE 配置文件。

# 附录 H 数据库版本管理

## H.1 版本化脚本目录

```
sql/
├── v3.0.0/              # 初始建表脚本
│   ├── init.sql         # 建库建表
│   ├── seed_data.sql    # 初始数据
│   └── indexes.sql      # 索引创建
├── v3.0.1/              # 增量升级脚本
│   ├── migration.sql    # 结构变更
│   └── rollback.sql     # 回滚脚本
├── v3.0.5/
│   ├── migration.sql
│   └── rollback.sql
└── ...
```

## H.2 版本管理规则

1. **每个版本一个目录**：每次数据库结构变更创建新版本目录；
2. **migration.sql + rollback.sql 成对**：必须同时提供升级和回滚脚本；
3. **版本号对应**：目录版本号与软件版本号一致；
4. **变更记录**：脚本内注释详细说明变更内容、变更原因、影响范围；
5. **执行顺序**：按版本号顺序依次执行，禁止跳过版本。

## H.3 数据迁移工具

使用 Alembic 管理数据库版本：
```bash
# 生成迁移脚本
alembic revision --autogenerate -m "add_quality_score_index"

# 执行迁移
alembic upgrade head

# 回滚到指定版本
alembic downgrade v3.0.4
```

